--- a/Kernel/KMain.HC
+++ b/Kernel/KMain.HC
@@ -186,6 +186,11 @@ U0 KMain()
   "Loading Compiler\n";
   Cd("/Compiler");
   Load("Compiler",LDF_SILENT);
   LBts(&sys_run_level,RLf_COMPILER);
+  {
+    U8 *mountin_message="MOUNTIN: RedSea /Compiler/Compiler.BIN loaded\n";
+    while (*mountin_message)
+      OutU8(0xE9,*mountin_message++);
+  }
   Spawn(&MsHardDrvrInstall);
