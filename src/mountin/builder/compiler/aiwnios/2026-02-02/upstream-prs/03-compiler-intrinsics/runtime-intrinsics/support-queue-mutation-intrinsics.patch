diff --git a/Src/AIWNIOS_CodeGen.HC b/Src/AIWNIOS_CodeGen.HC
index e39ca4c..806655d 100644
--- a/Src/AIWNIOS_CodeGen.HC
+++ b/Src/AIWNIOS_CodeGen.HC
@@ -104,6 +104,9 @@ import U8 *__HC_ICAdd_OutU8(U8*);
 import U8 *__HC_ICAdd_OutU16(U8*);
 import U8 *__HC_ICAdd_OutU32(U8*);
 import U8 *__HC_ICAdd_QueInit(U8*);
+import U8 *__HC_ICAdd_QueIns(U8*);
+import U8 *__HC_ICAdd_QueInsRev(U8*);
+import U8 *__HC_ICAdd_QueRem(U8*);
 import U8 *__HC_ICAdd_InvlPg(U8*);
 import U8 *__HC_ICAdd_StrLen(U8*);
 import U8 *__HC_ICAdd_Ret(U8*);
@@ -220,6 +223,9 @@ extern U8 *__HC_ICAdd_OutU8(U8*);
 extern U8 *__HC_ICAdd_OutU16(U8*);
 extern U8 *__HC_ICAdd_OutU32(U8*);
 extern U8 *__HC_ICAdd_QueInit(U8*);
+extern U8 *__HC_ICAdd_QueIns(U8*);
+extern U8 *__HC_ICAdd_QueInsRev(U8*);
+extern U8 *__HC_ICAdd_QueRem(U8*);
 extern U8 *__HC_ICAdd_InvlPg(U8*);
 extern U8 *__HC_ICAdd_StrLen(U8*);
 extern U8 *__HC_ICAdd_Ret(U8*);
@@ -2033,6 +2039,12 @@ break;case IC_RIP:
     new=__HC_ICAdd_OutU32(cc2);
     break;case IC_QUE_INIT:
     new=__HC_ICAdd_QueInit(cc2);
+    break;case IC_QUE_INS:
+    new=__HC_ICAdd_QueIns(cc2);
+    break;case IC_QUE_INS_REV:
+    new=__HC_ICAdd_QueInsRev(cc2);
+    break;case IC_QUE_REM:
+    new=__HC_ICAdd_QueRem(cc2);
     break;case IC_INVLPG:
     new=__HC_ICAdd_InvlPg(cc2);
     break;case IC_STRLEN:
diff --git a/c/aiwn_lexparser.h b/c/aiwn_lexparser.h
index 7c99589..c10e534 100644
--- a/c/aiwn_lexparser.h
+++ b/c/aiwn_lexparser.h
@@ -251,6 +251,9 @@ enum {
   IC_OUT_U16,
   IC_OUT_U32,
   IC_QUE_INIT,
+  IC_QUE_INS,
+  IC_QUE_INS_REV,
+  IC_QUE_REM,
   IC_INVLPG,
   IC_STRLEN,
   IC_POS,
@@ -706,6 +709,9 @@ HC_IC_BINDINGH(HC_ICAdd_OutU8)
 HC_IC_BINDINGH(HC_ICAdd_OutU16)
 HC_IC_BINDINGH(HC_ICAdd_OutU32)
 HC_IC_BINDINGH(HC_ICAdd_QueInit)
+HC_IC_BINDINGH(HC_ICAdd_QueIns)
+HC_IC_BINDINGH(HC_ICAdd_QueInsRev)
+HC_IC_BINDINGH(HC_ICAdd_QueRem)
 HC_IC_BINDINGH(HC_ICAdd_InvlPg)
 HC_IC_BINDINGH(HC_ICAdd_StrLen)
 HC_IC_BINDINGH(HC_ICAdd_And)
diff --git a/c/main.c b/c/main.c
index d319887..f4af49f 100644
--- a/c/main.c
+++ b/c/main.c
@@ -1221,6 +1221,15 @@ static int64_t STK___HC_ICAdd_OutU32(int64_t *stk) { return __HC_ICAdd_OutU32(st
 static int64_t STK___HC_ICAdd_QueInit(int64_t *stk) {
   return __HC_ICAdd_QueInit(stk[0]);
 }
+static int64_t STK___HC_ICAdd_QueIns(int64_t *stk) {
+  return __HC_ICAdd_QueIns(stk[0]);
+}
+static int64_t STK___HC_ICAdd_QueInsRev(int64_t *stk) {
+  return __HC_ICAdd_QueInsRev(stk[0]);
+}
+static int64_t STK___HC_ICAdd_QueRem(int64_t *stk) {
+  return __HC_ICAdd_QueRem(stk[0]);
+}
 static int64_t STK___HC_ICAdd_InvlPg(int64_t *stk) {
   return __HC_ICAdd_InvlPg(stk[0]);
 }
@@ -1853,6 +1862,9 @@ static void BootAiwnios(char *bootstrap_text) {
     PrsAddSymbol("__HC_ICAdd_OutU16", STK___HC_ICAdd_OutU16, 1);
     PrsAddSymbol("__HC_ICAdd_OutU32", STK___HC_ICAdd_OutU32, 1);
     PrsAddSymbol("__HC_ICAdd_QueInit", STK___HC_ICAdd_QueInit, 1);
+    PrsAddSymbol("__HC_ICAdd_QueIns", STK___HC_ICAdd_QueIns, 1);
+    PrsAddSymbol("__HC_ICAdd_QueInsRev", STK___HC_ICAdd_QueInsRev, 1);
+    PrsAddSymbol("__HC_ICAdd_QueRem", STK___HC_ICAdd_QueRem, 1);
     PrsAddSymbol("__HC_ICAdd_InvlPg", STK___HC_ICAdd_InvlPg, 1);
     PrsAddSymbol("__HC_ICAdd_StrLen", STK___HC_ICAdd_StrLen, 1);
     PrsAddSymbol("__HC_ICAdd_Ret", STK___HC_ICAdd_Ret, 1);
diff --git a/c/optpass.c b/c/optpass.c
index 675b21a..699b31c 100644
--- a/c/optpass.c
+++ b/c/optpass.c
@@ -704,6 +704,7 @@ loop:
       break;
     case IC_DEREF:
     case IC_QUE_INIT:
+    case IC_QUE_REM:
     case IC_INVLPG:
     case IC_STRLEN:
     case IC_IN_U8:
@@ -714,6 +715,10 @@ loop:
     case IC_OUT_U32:
       goto next1;
       break;
+    case IC_QUE_INS:
+    case IC_QUE_INS_REV:
+      goto next1;
+      break;
     case IC_AND:
       if (IsConst(next = rpn->base.next) && IsConst(ICFwd(rpn->base.next))) {
         if (rpn->raw_type == RT_F64) {
@@ -1140,6 +1145,9 @@ void OptPassRegAlloc(CCmpCtrl *cctrl) {
       break;
     case IC_DEREF:
     case IC_QUE_INIT:
+    case IC_QUE_INS:
+    case IC_QUE_INS_REV:
+    case IC_QUE_REM:
     case IC_INVLPG:
     case IC_STRLEN:
     case IC_IN_U8:
diff --git a/c/parser.c b/c/parser.c
index 2dccbb9..0ea3d4a 100644
--- a/c/parser.c
+++ b/c/parser.c
@@ -244,6 +244,7 @@ CRPN *ICFwd(CRPN *rpn) {
     break;
   case IC_TYPECAST:
   case IC_QUE_INIT:
+  case IC_QUE_REM:
   case IC_INVLPG:
   case IC_STRLEN:
   case IC_IN_U8:
@@ -251,6 +252,10 @@ CRPN *ICFwd(CRPN *rpn) {
   case IC_IN_U32:
     goto unop;
     break;
+  case IC_QUE_INS:
+  case IC_QUE_INS_REV:
+    goto binop;
+    break;
   case IC_COMMA:
   case IC_MAX_F64:
   case IC_MAX_U64:
@@ -341,7 +346,6 @@ CRPN *ICFwd(CRPN *rpn) {
   case IC_SQR:
   case IC_POS:
     goto unop;
-    break;
   case IC_NAME:
     return rpn->base.next;
     break;
@@ -815,6 +819,15 @@ CRPN *ParserDumpIR(CRPN *rpn, int64_t indent) {
     printf("QUE_INIT\n");
     goto unop;
     break;
+  case IC_QUE_INS:
+    printf("QUE_INS\n");
+    goto binop;
+  case IC_QUE_INS_REV:
+    printf("QUE_INS_REV\n");
+    goto binop;
+  case IC_QUE_REM:
+    printf("QUE_REM\n");
+    goto unop;
   case IC_INVLPG:
     printf("INVLPG\n");
     goto unop;
@@ -1361,6 +1374,15 @@ int64_t DolDocDumpIR(char *to, int64_t len, CRPN *rpn) {
     len = AddBytes(to, len, "$PURPLE$QUE_INIT$FD$\n");
     goto unop;
     break;
+  case IC_QUE_INS:
+    len = AddBytes(to, len, "$PURPLE$QUE_INS$FD$\n");
+    goto binop;
+  case IC_QUE_INS_REV:
+    len = AddBytes(to, len, "$PURPLE$QUE_INS_REV$FD$\n");
+    goto binop;
+  case IC_QUE_REM:
+    len = AddBytes(to, len, "$PURPLE$QUE_REM$FD$\n");
+    goto unop;
   case IC_INVLPG:
     len = AddBytes(to, len, "$PURPLE$INVLPG$FD$\n");
     goto unop;
@@ -3527,6 +3549,13 @@ int64_t AssignRawTypeToNode(CCmpCtrl *ccmp, CRPN *rpn) {
     return rpn->raw_type = RT_U0; // dummy
     break;
   case IC_QUE_INIT:
+  case IC_QUE_REM:
+    AssignRawTypeToNode(ccmp, ICArgN(rpn, 0));
+    return rpn->raw_type = RT_U0;
+    break;
+  case IC_QUE_INS:
+  case IC_QUE_INS_REV:
+    AssignRawTypeToNode(ccmp, ICArgN(rpn, 1));
     AssignRawTypeToNode(ccmp, ICArgN(rpn, 0));
     return rpn->raw_type = RT_U0;
     break;
@@ -3689,7 +3718,6 @@ int64_t AssignRawTypeToNode(CCmpCtrl *ccmp, CRPN *rpn) {
     break;
   case IC_POS:
     goto unop;
-    break;
   case IC_NAME:
     abort();
     break;
@@ -4732,6 +4760,9 @@ HC_IC_BINDING(HC_ICAdd_OutU8, IC_OUT_U8);
 HC_IC_BINDING(HC_ICAdd_OutU16, IC_OUT_U16);
 HC_IC_BINDING(HC_ICAdd_OutU32, IC_OUT_U32);
 HC_IC_BINDING(HC_ICAdd_QueInit, IC_QUE_INIT);
+HC_IC_BINDING(HC_ICAdd_QueIns, IC_QUE_INS);
+HC_IC_BINDING(HC_ICAdd_QueInsRev, IC_QUE_INS_REV);
+HC_IC_BINDING(HC_ICAdd_QueRem, IC_QUE_REM);
 HC_IC_BINDING(HC_ICAdd_InvlPg, IC_INVLPG);
 HC_IC_BINDING(HC_ICAdd_StrLen, IC_STRLEN);
 HC_IC_BINDING(HC_ICAdd_And, IC_AND);
diff --git a/c/x86_64_backend.c b/c/x86_64_backend.c
index 9349636..a062b3e 100644
--- a/c/x86_64_backend.c
+++ b/c/x86_64_backend.c
@@ -2269,6 +2269,7 @@ static int64_t SpillsTmpRegs(CRPN *rpn) {
   case IC_BSF:
   case IC_BSR:
   case IC_QUE_INIT:
+  case IC_QUE_REM:
   case IC_INVLPG:
   case IC_STRLEN:
   case IC_SQRT:
@@ -2284,6 +2285,9 @@ static int64_t SpillsTmpRegs(CRPN *rpn) {
     break;
   case IC_POW:
     goto pass; // calls pow
+  case IC_QUE_INS:
+  case IC_QUE_INS_REV:
+    goto binop;
   binop:
     if (SpillsTmpRegs(rpn->base.next))
       goto pass;
@@ -2951,6 +2955,7 @@ static int64_t PushTmpDepthFirst(CCmpCtrl *cctrl, CRPN *r, int64_t spilled) {
   case IC_BSF:
   case IC_BSR:
   case IC_QUE_INIT:
+  case IC_QUE_REM:
   case IC_INVLPG:
   case IC_STRLEN:
   case IC_IN_U8:
@@ -2965,6 +2970,8 @@ static int64_t PushTmpDepthFirst(CCmpCtrl *cctrl, CRPN *r, int64_t spilled) {
   case IC_GT:
   case IC_GE:
   case IC_LE:
+  case IC_QUE_INS:
+  case IC_QUE_INS_REV:
   cmp_binop:
     arg = ICArgN(r, 1);
     arg2 = ICArgN(r, 0);
@@ -6219,6 +6226,7 @@ static void SetKeepTmps(CRPN *rpn) {
   case IC_BSF:
   case IC_BSR:
   case IC_QUE_INIT:
+  case IC_QUE_REM:
   case IC_INVLPG:
   case IC_STRLEN:
   case IC_IN_U8:
@@ -6249,6 +6257,8 @@ static void SetKeepTmps(CRPN *rpn) {
   case IC_LBTR:
   case IC_LBTC:
   case IC_STORE:
+  case IC_QUE_INS:
+  case IC_QUE_INS_REV:
   case IC_OUT_U8:
   case IC_OUT_U16:
   case IC_OUT_U32:
@@ -6462,6 +6472,9 @@ int64_t __OptPassFinal(CCmpCtrl *cctrl, CRPN *rpn, char *bin,
       [IC_OUT_U16] = &&ic_out_u16,
       [IC_OUT_U32] = &&ic_out_u32,
       [IC_QUE_INIT] = &&ic_que_init,
+      [IC_QUE_INS] = &&ic_que_ins,
+      [IC_QUE_INS_REV] = &&ic_que_ins_rev,
+      [IC_QUE_REM] = &&ic_que_rem,
       [IC_INVLPG] = &&ic_invlpg,
       [IC_STRLEN] = &&ic_strlen,
       [IC_RAW_BYTES] = &&ic_raw_bytes,
@@ -6559,6 +6572,55 @@ int64_t __OptPassFinal(CCmpCtrl *cctrl, CRPN *rpn, char *bin,
     AIWNIOS_ADD_CODE(X86MovIndirRegI64, MIR(cctrl, into_reg), -1, -1,
                      MIR(cctrl, into_reg), 8);
     break;
+  ic_que_ins:
+  ic_que_ins_rev:
+    next = ICArgN(rpn, 1);
+    next2 = ICArgN(rpn, 0);
+    next->res.keep_in_tmp = 1;
+    next2->res.keep_in_tmp = 1;
+    code_off = __OptPassFinal(cctrl, next, bin, code_off);
+    code_off = PushToStack(cctrl, &next->res, bin, code_off);
+    code_off = __OptPassFinal(cctrl, next2, bin, code_off);
+    code_off = PushToStack(cctrl, &next2->res, bin, code_off);
+    AIWNIOS_ADD_CODE(X86PopReg, MIR(cctrl, RDX));
+    AIWNIOS_ADD_CODE(X86PopReg, MIR(cctrl, RAX));
+    a_reg = RAX;
+    b_reg = RDX;
+    if (rpn->type == IC_QUE_INS_REV) {
+      AIWNIOS_ADD_CODE(X86MovRegIndirI64, MIR(cctrl, RCX), -1, -1,
+                       MIR(cctrl, b_reg), 8);
+    } else {
+      AIWNIOS_ADD_CODE(X86MovRegReg, MIR(cctrl, RCX), MIR(cctrl, b_reg));
+    }
+    AIWNIOS_ADD_CODE(X86MovRegIndirI64, MIR(cctrl, R8), -1, -1,
+                     MIR(cctrl, RCX), 0);
+    AIWNIOS_ADD_CODE(X86MovIndirRegI64, MIR(cctrl, RCX), -1, -1,
+                     MIR(cctrl, a_reg), 8);
+    AIWNIOS_ADD_CODE(X86MovIndirRegI64, MIR(cctrl, R8), -1, -1,
+                     MIR(cctrl, a_reg), 0);
+    AIWNIOS_ADD_CODE(X86MovIndirRegI64, MIR(cctrl, a_reg), -1, -1,
+                     MIR(cctrl, RCX), 0);
+    AIWNIOS_ADD_CODE(X86MovIndirRegI64, MIR(cctrl, a_reg), -1, -1,
+                     MIR(cctrl, R8), 8);
+    break;
+  ic_que_rem:
+    next = ICArgN(rpn, 0);
+    next->res.keep_in_tmp = 1;
+    code_off = __OptPassFinal(cctrl, next, bin, code_off);
+    tmp.mode = MD_REG;
+    tmp.raw_type = RT_PTR;
+    tmp.reg = RAX;
+    code_off = ICMov(cctrl, &tmp, &next->res, bin, code_off);
+    into_reg = RAX;
+    AIWNIOS_ADD_CODE(X86MovRegIndirI64, MIR(cctrl, RDX), -1, -1,
+                     MIR(cctrl, into_reg), 0);
+    AIWNIOS_ADD_CODE(X86MovRegIndirI64, MIR(cctrl, RCX), -1, -1,
+                     MIR(cctrl, into_reg), 8);
+    AIWNIOS_ADD_CODE(X86MovIndirRegI64, MIR(cctrl, RDX), -1, -1,
+                     MIR(cctrl, RCX), 0);
+    AIWNIOS_ADD_CODE(X86MovIndirRegI64, MIR(cctrl, RCX), -1, -1,
+                     MIR(cctrl, RDX), 8);
+    break;
   ic_rdtsc:
     AIWNIOS_ADD_CODE(X86Rdtsc, 0);
     AIWNIOS_ADD_CODE(X86ShlImm, RDX, 32);
diff --git a/Tests/Compiler/Queues.HC b/Tests/Compiler/Queues.HC
new file mode 100644
--- /dev/null
+++ b/Tests/Compiler/Queues.HC
@@ -0,0 +1,20 @@
+public _intern IC_QUE_INIT U0 TestInit(CQue *q);
+public _intern IC_QUE_INS U0 TestInsert(CQue *a,CQue *b);
+public _intern IC_QUE_INS_REV U0 TestInsertRev(CQue *a,CQue *b);
+public _intern IC_QUE_REM U0 TestRemove(CQue *q);
+CQue head,one,two;
+CQue *TestOne() {return &one;}
+CQue *TestHead() {return &head;}
+TestInit(&head);
+TestInsert(TestOne(),TestHead());
+if (head.next!=&one || one.last!=&head ||
+      one.next!=&head || head.last!=&one) throw('QueueIns');
+TestInsertRev(&two,TestHead());
+if (head.last!=&two || two.next!=&head ||
+      two.last!=&one || one.next!=&two) throw('QueueRev');
+TestRemove(TestOne());
+if (head.next!=&two || two.last!=&head) throw('QueueRem');
+TestRemove(&two);
+if (head.next!=&head || head.last!=&head) throw('QueueEnd');
+"PASS: queues\n";
+Exit;
