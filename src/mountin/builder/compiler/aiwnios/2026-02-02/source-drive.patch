diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/AIWNIOS_CodeGen.HC b/Src/AIWNIOS_CodeGen.HC
--- a/Src/AIWNIOS_CodeGen.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/AIWNIOS_CodeGen.HC	2026-09-02 00:02:45.000000000 +0100
@@ -2275,6 +2275,9 @@
     if(cur->user_data2) I64SetDel(cur->user_data2);
     if(cur->type==IC_ASM) {
 //TODO AOT
+      if(cc->flags&CCF_AOT_COMPILE)
+        cur->ic_data(CAiwniosAOTBlob*)->final_start_rip=cc->aotc->rip+
+              (cur->ic_data(CAiwniosAOTBlob*)->final_start_addr-machine_code);
       JITFixupAiwniosAOTBlob(cc,cur->ic_data,cur->ic_data(CAiwniosAOTBlob*)->final_start_addr);
       AiwniosAOTBlobDel(cur->ic_data);
     }
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/AIWNIOS_PrsExp.HC b/Src/AIWNIOS_PrsExp.HC
--- a/Src/AIWNIOS_PrsExp.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/AIWNIOS_PrsExp.HC	2026-09-02 00:02:45.000000000 +0100
@@ -833,6 +833,19 @@
   return cur;
 }
 
+U0 AsmExpressionRelocAdd(CCmpCtrl *cc,CHash *h)
+{
+  CAsmUndefHash *tmpauh;
+
+  if (cc->flags&CCF_ASM_EXPRESSIONS) {
+    tmpauh=CAlloc(sizeof CAsmUndefHash);
+    tmpauh->hash=h;
+    tmpauh->next=cc->asm_undef_hash;
+    cc->asm_undef_hash=tmpauh;
+    cc->flags|=CCF_UNRESOLVED;
+  }
+}
+
 U0 DbgPrint(U8 *fmt,...) {
   U8 *s=StrPrintJoin(NULL,fmt,argc,argv);
   DbgPutS(s);
@@ -996,6 +996,8 @@
   CHashClass *tmpc;
   CMemberLst *mlst;
   CAsmUndefHash *tmpauh;
+  U8 *lookahead;
+  Bool aot_fun_import;
 //u32[0] is type
   //u32[1] is prec
   CRPN *rpn,*rpn2;
@@ -1425,6 +1426,24 @@
         break;start:
         if(!last_consumes_right)
 	  finish=TRUE;
+        case '\d':
+	  if(finish) break;
+	  if(cc->flags&CCF_ASM_EXPRESSIONS) {
+	    cc->abs_cnts.abs_addres++;
+	    if(cc->flags&CCF_AOT_COMPILE)
+	      RPNAddNew(cc,IC_ABS_ADDR,cc->aotc->rip,
+	        cmp.internal_types[RT_PTR]);
+	    else
+	      RPNAddNew(cc,IC_IMM_I64,cc->aotc->rip,
+	        cmp.internal_types[RT_PTR]);
+	  } else if(cc->flags&CCF_CLASS_DOL_OFFSET)
+	    RPNAddNew(cc,IC_IMM_I64,cc->class_dol_offset,
+	      cmp.internal_types[RT_I64]);
+	  else
+	    RPNAddNew(cc,IC_RIP,0,cmp.internal_types[RT_PTR]);
+	  last_consumes_right=0;
+	  Lex(cc);
+	  break;
         case TK_STR:
 	  if(finish) break;
 	  cc->flags|=CCF_HAS_MISC_DATA;
@@ -1459,6 +1471,12 @@
 	  last_consumes_right=0;
 	  Lex(cc);
 	  break;case TK_IDENT:
+	  if(cc->flags&CCF_ASM_EXPRESSIONS&&
+        HashFind(cc->cur_str,cmp.asm_hash,HTT_OPCODE|HTT_ASM_KEYWORD)) {
+	    finish=TRUE;
+	    break;
+	  }
	  if(finish) break;
	  last_consumes_right=0;
+          aot_fun_import=FALSE;
	  if(cc->flags&CCF_ASM_EXPRESSIONS) {
@@ -1475,10 +1493,27 @@
	      goto ins_label;
-	    } else if(cc->hash_entry&&cc->hash_entry->type&HTT_CLASS) {
+	    } else if(cc->aot&&cc->hash_entry&&
+                  cc->hash_entry->type&HTT_FUN&&
+                  !Bt(&cc->hash_entry(CHashFun*)->flags,Ff_INTERNAL)) {
+              aot_fun_import=TRUE;
+              cc->hash_entry=NULL;
+              goto new;
+            } else if(cc->hash_entry&&cc->hash_entry->type&HTT_CLASS) {
 //MOV RAX, CTask.addr[RBX]
-	      PrsOffsetOf(cc);
-	      break;
+              lookahead=cc->cur_buf_ptr;
+              while(*lookahead&&*lookahead<=' ') lookahead++;
+              if(cc->flags&CCF_USE_LAST_U16&&cc->last_U16=='.'||
+                    *lookahead=='.') {
+	        PrsOffsetOf(cc);
+	        break;
+              }
+              cc->hash_entry=NULL;
+              goto new;
 	    } else if(!cc->hash_entry) {
 new:
+	      if(flags&PEF_ADDR_OF) {
+	        PrsPop(cc->ps);
+	        RPNDel(PrsPop2(cc->ps));
+	      }
 	      tmpex=CAlloc(sizeof(CHashExport));
	      tmpex->str=StrNew(cc->cur_str);
	      if (*tmpex->str=='@' && tmpex->str[1]=='@') {
@@ -1496,10 +1531,12 @@
 	        tmpex->type=HTT_EXPORT_SYS_SYM | HTF_UNRESOLVED;
 ins_label:
-	        tmpc=HashFind(tmpex->str,cc->htc.glbl_hash_table,HTT_EXPORT_SYS_SYM);
-	        if (tmpc)
-		  HashAddAfter(tmpex,tmpc,cc->htc.glbl_hash_table);
-	        else
-		  HashAdd(tmpex,cc->htc.glbl_hash_table);
+                if (!aot_fun_import) {
+	          tmpc=HashFind(tmpex->str,cc->htc.glbl_hash_table,HTT_EXPORT_SYS_SYM);
+	          if (tmpc)
+		    HashAddAfter(tmpex,tmpc,cc->htc.glbl_hash_table);
+	          else
+		    HashAdd(tmpex,cc->htc.glbl_hash_table);
+                }
 	      }
 	      cc->flags|=CCF_UNRESOLVED;
 	      rpn=RPNAddNew(
@@ -1520,6 +1561,7 @@
 		    CodeMiscHashNew(cc,cc->hash_entry),
 		    cmp.internal_types[RT_I64],
 		    );
+	      AsmExpressionRelocAdd(cc,cc->hash_entry);
 	      Lex(cc);
 	      goto fin_ident;
 	    } else if(cc->hash_entry->type&HTT_EXPORT_SYS_SYM) {
@@ -1570,6 +1612,7 @@
 		      CodeMiscHashNew(cc,cc->hash_entry),
 		      cc->hash_entry,
 		      );
+	          AsmExpressionRelocAdd(cc,cc->hash_entry);
 		      #ifdef TARGET_RISCV
 		      rpn->ic_data(CCodeMisc*)->flags|=CMF_SHORT_ADDR;
 		      #endif
@@ -1652,6 +1695,7 @@
 		      CodeMiscHashNew(cc,cc->hash_entry),
 		      cc->hash_entry(CHashGlblVar*)->var_class,
 		      );
+	        AsmExpressionRelocAdd(cc,cc->hash_entry);
 	      } else {
 		  rpn=RPNAddNew(
 		      cc,
@@ -1791,5 +1818,9 @@
   COCInit(cc);
   if (PrsExpression(cc,NULL,PEF_NO_COMMA)) {
+    if (cc->flags&CCF_ASM_EXPRESSIONS) {
+      DbgPrint("MOUNTIN RPN\n");
+      DumpRPN(cc->coc.coc_head.next);
+    }
     RPNAddNew(cc,IC_RET,0,AssignRawTypeToNode(cc->coc.coc_head.next),FALSE);
     old_fun=cc->htc.fun; //Changed by nroot
     cc->htc.fun=NULL;
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/AsmResolve.HC b/Src/AsmResolve.HC
--- a/Src/AsmResolve.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/AsmResolve.HC	2026-09-02 00:02:45.000000000 +0100
@@ -1,3 +1,76 @@
+U0 AsmUnresolvedAdd(CCmpCtrl *cc,U8 *machine_code,I64 type,I64 rip,I64 rel_rip,
+      CAsmUndefHash *local_asm_undef_hash,CAsmUndefHash *glbl_asm_undef_hash,
+      I64 line_num,Bool U8_avail)
+{
+  CAsmUnresolvedRef *tmpu=MAlloc(sizeof(CAsmUnresolvedRef));
+  tmpu->machine_code=machine_code;
+  tmpu->type=type;
+  tmpu->rip=rip;
+  tmpu->rel_rip=rel_rip;
+  tmpu->aot=cc->aot;
+  tmpu->imm_flag=FALSE;
+  tmpu->line_num=line_num;
+  tmpu->U8_avail=U8_avail;
+  tmpu->str=NULL;
+  if (local_asm_undef_hash) {
+    tmpu->asm_undef_hash=local_asm_undef_hash;
+    tmpu->next=cc->aotc->local_unresolved;
+    cc->aotc->local_unresolved=tmpu;
+  } else {
+    tmpu->asm_undef_hash=glbl_asm_undef_hash;
+    tmpu->next=cc->aotc->glbl_unresolved;
+    cc->aotc->glbl_unresolved=tmpu;
+    if (glbl_asm_undef_hash->hash->type & HTF_IMPORT) {
+      tmpu->str=StrNew(glbl_asm_undef_hash->hash->str);
+      if (glbl_asm_undef_hash->hash->type & HTF_IMM)
+        tmpu->imm_flag=TRUE;
+    }
+  }
+}
+
+U0 AsmResolve(CCmpCtrl *cc,CAsmUnresolvedRef *tmpu,U8 *label,Bool undefined)
+{
+  CAOTImportExport *tmpie;
+  CAsmUndefHash *tmpauh;
+  I64 (*machine_code)()=tmpu->machine_code;
+  I64 res=(*machine_code)();
+  if (undefined) {
+    tmpauh=tmpu->asm_undef_hash;
+    while (tmpauh) {
+      if (tmpauh->hash->type & HTF_UNRESOLVED) {
+        tmpie=CAlloc(sizeof(CAOTImportExport));
+        tmpie->next=tmpauh->hash->ie_lst;
+        tmpauh->hash->ie_lst=tmpie;
+        tmpie->rip=tmpu->rip;
+        tmpie->aot=cc->aot;
+        tmpie->type=tmpu->type;
+      }
+      tmpauh=tmpauh->next;
+    }
+  } else if (!(tmpu->type&IEF_IMM_NOT_REL)) {
+    res-=tmpu->rel_rip;
+    if (tmpu->type==IET_REL_I8 && !(I8_MIN<=res<=I8_MAX) ||
+          tmpu->type==IET_REL_I16 && !(I16_MIN<=res<=I16_MAX)) {
+      PrintErr("Branch out of range at line:%04d %s\n",
+            tmpu->line_num,label);
+      LexExcept(cc);
+    }
+    if (tmpu->U8_avail && tmpu->type>IET_IMM_U8 && -124<=res<=123)
+      PrintWarn("could use I8 displacement at line:%04d %s %s\n",
+            tmpu->line_num,cc->aotc->last_label,label);
+  }
+  AOTStoreCodeU8At(cc,tmpu->rip,res.u8[0]);
+  if (tmpu->type>=IET_REL_I16) {
+    AOTStoreCodeU8At(cc,tmpu->rip+1,res.u8[1]);
+    if (tmpu->type>=IET_REL_I32) {
+      AOTStoreCodeU8At(cc,tmpu->rip+2,res.u8[2],res.u8[3]);
+      if (tmpu->type>=IET_REL_I64)
+        AOTStoreCodeU8At(cc,tmpu->rip+4,res.u8[4],res.u8[5],
+              res.u8[6],res.u8[7]);
+    }
+  }
+}
+
 U0 AOTGlblsResolve(CCmpCtrl *cc,CAOT *tmpaot)
 {
   CAOTCtrl *aotc=cc->aotc;
@@ -11,12 +84,16 @@
   Bool undefined;
   CExternUsage *tmpeu,*tmpeu8;
 
   while (tmpu) {
+    Print("MOUNTIN AOT ref=%X next=%X code=%X hash=%X type=%d rip=%X rel=%X\\n",
+          tmpu,tmpu->next,tmpu->machine_code,tmpu->asm_undef_hash,
+          tmpu->type,tmpu->rip,tmpu->rel_rip);
     label=NULL;
     undefined=FALSE;
     tmpu1=tmpu->next;
     tmpauh=tmpu->asm_undef_hash;
     while (tmpauh) {
+      Print("MOUNTIN AOT hash-node=%X hash=%X\\n",tmpauh,tmpauh->hash);
       if (tmpauh->hash->type & HTF_UNRESOLVED) {
 	tmpex=tmpauh->hash;
 	if (tmpex->type & HTT_EXPORT_SYS_SYM&& tmpex->type & HTF_UNRESOLVED &&
@@ -48,7 +125,10 @@
 	label=tmpauh->hash->str;
       tmpauh=tmpauh->next;
     }
-    //AsmResolve(cc,tmpu,label,undefined); TODO restore ???
+    Print("MOUNTIN AOT resolving ref=%X label=%s undefined=%d\\n",
+          tmpu,label,undefined);
+    AsmResolve(cc,tmpu,label,undefined);
+    Print("MOUNTIN AOT resolved ref=%X\\n",tmpu);
     Free(tmpu->machine_code);
     LinkedLstDel(tmpu->asm_undef_hash);
     Free(tmpu);
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/AsmX64.HC b/Src/AsmX64.HC
--- a/Src/AsmX64.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/AsmX64.HC	2026-09-02 01:43:40.797065620 +0100
@@ -10,7 +10,8 @@
   I64 len;
   U8 *data;
   //private,used when placing ->data into it's final location
-  U8 **final_start_addr;
+  I64 final_start_rip;
+  U8 *final_start_addr;
 };
 U0 AiwniosAOTBlobDel(CAiwniosAOTBlob *b) {
   QueDel(b->asm_patches);
@@ -62,6 +63,8 @@
 Bool PrsAsmImm(CCmpCtrl *cc,CX86AddrMode *arg)
 {
   I64 old_flags=cc->flags;
+  I64 (*machine_code)();
+  I64 i,start_token=cc->token,start_i64=cc->cur_i64;
   arg->local_asm_undef_hash=NULL;
   arg->glbl_asm_undef_hash=NULL;
   cc->asm_undef_hash=NULL;
@@ -82,7 +85,18 @@
 	cc->asm_undef_hash=NULL;
       }
     } else {
-      arg->imm=(*arg->machine_code)();
+      machine_code=arg->machine_code;
+      Print("MOUNTIN EVAL token=%d cur_i64=%X flags=%X abs=%d code=%X bytes=",
+            start_token,start_i64,cc->flags,cc->abs_cnts.abs_addres,machine_code);
+      for (i=0;i<24;i++) Print("%02X",machine_code(U8*)[i]);
+      "\n";
+      arg->imm=(*machine_code)();
+      if (arg->imm>I32_MAX||arg->imm<I32_MIN) {
+        Print("MOUNTIN IMM token=%d cur_i64=%X result=%X i64class=%X code=%X bytes=",
+              start_token,start_i64,arg->imm,cmp.internal_types[RT_I64],machine_code);
+        for (i=0;i<24;i++) Print("%02X",machine_code(U8*)[i]);
+        "\n";
+      }
       arg->glbl_asm_undef_hash=cc->asm_undef_hash;
       cc->asm_undef_hash=NULL;
       Free(arg->machine_code);
@@ -95,7 +110,10 @@
 
 CX86AddrMode *PrsX86AddrMode(CCmpCtrl *cc) {
   CX86AddrMode *mode=CAlloc(sizeof CX86AddrMode);
-  CHashReg *rg;
+  CHashReg *rg=NULL;
+  CHashClass *tmpc=NULL;
+  CMemberLst *tmpm;
+  U8 *lookahead;
   I64 (*machine_code)(),cur_i64;
   mode->scale=-1;
   mode->type=-1;
@@ -108,13 +112,58 @@
   mode->imm=0;
   mode->label=NULL;
+prefix:
   if(cc->token==TK_IDENT) {
-    if(cc->hash_entry&&cc->hash_entry->type&(HTT_CLASS|HTT_INTERNAL_TYPE)) {
-      mode->operand_sz=cc->hash_entry(CHashClass*)->size;
+    tmpc=HashFind(cc->cur_str,cmp.asm_hash,HTT_INTERNAL_TYPE);
+    if(tmpc) {
+      mode->operand_sz=tmpc->size;
+      Lex(cc);
+      goto size_prefix_consumed;
+    }
+  }
+  if(cc->token==TK_IDENT||cc->hash_entry&&
+        cc->hash_entry->type&HTT_INTERNAL_TYPE) {
+    lookahead=cc->cur_buf_ptr;
+    while(*lookahead&&*lookahead<=' ') lookahead++;
+    rg=HashFind(cc->cur_str,cmp.asm_hash,HTT_REG);
+    tmpc=HashFind(cc->cur_str,cmp.asm_hash,HTT_INTERNAL_TYPE);
+    if(!tmpc&&cc->hash_entry&&cc->hash_entry->type&HTT_INTERNAL_TYPE)
+      tmpc=cc->hash_entry;
+    if(!rg&&!tmpc&&cc->hash_entry&&!StrCmp(cc->cur_str,cc->hash_entry->str)&&
+          cc->hash_entry->type&HTT_CLASS&&
+          cc->hash_entry(CHashClass*)->member_lst_and_root&&
+          (cc->flags&CCF_USE_LAST_U16&&cc->last_U16=='.'||*lookahead=='.')) {
+      tmpc=cc->hash_entry;
+      if(Lex(cc)!='.')
+        LexExcept(cc,"Expected a class member at");
+      mode->off=0;
+      do {
+        if(Lex(cc)!=TK_IDENT || !(tmpm=MemberFind(cc->cur_str,tmpc)))
+          LexExcept(cc,"Invalid class member at");
+        Print("MOUNTIN MEMBER %s.%s offset=%d\\n",tmpc->str,tmpm->str,tmpm->offset);
+        mode->off+=tmpm->offset;
+        tmpc=tmpm->member_class;
+      } while(Lex(cc)=='.');
+      if(cc->token=='+') {
+        Lex(cc);
+        cur_i64=mode->off;
+        mode->type=X86MT_IMM;
+        PrsAsmImm(cc,mode);
+        if(mode->local_asm_undef_hash||mode->glbl_asm_undef_hash)
+          LexExcept(cc,"Class member displacement must be resolved.");
+        mode->imm=cur_i64+mode->imm;
+      }
+      if(cc->token=='[')
+        goto indir;
+      mode->type=X86MT_IMM;
+      goto ret;
+    } else if(!rg&&tmpc) {
+      mode->operand_sz=tmpc->size;
       Lex(cc);
-      goto indir;
     }
-    if(cc->hash_entry&&cc->hash_entry->type&HTT_REG) {
-      rg=cc->hash_entry;
+size_prefix_consumed:
+    if(!rg&&cc->token==TK_IDENT)
+      rg=HashFind(cc->cur_str,cmp.asm_hash,HTT_REG);
+    if(rg) {
       Lex(cc);
       if(cc->token==':') {
         Lex(cc);
@@ -123,7 +174,8 @@
 	  LexExcept(cc,"Expected a segment register at");
         }
         mode->seg=rg->reg_num;
-        goto indir;
+        rg=NULL;
+        goto prefix;
       }
       mode->r=rg->reg_num;
       mode->reg_type=rg->reg_type;
@@ -143,8 +193,8 @@
       default:
         goto check_imm;
       case TK_IDENT:
-        if(cc->hash_entry&&cc->hash_entry->type&HTT_REG) {
-	  rg=cc->hash_entry;
+        rg=HashFind(cc->cur_str,cmp.asm_hash,HTT_REG);
+        if(rg) {
 	  Lex(cc);
 	  if(cc->token=='+') {
 	    Lex(cc);
@@ -323,19 +373,19 @@
     case X86_OPERAND_IMM8:
       if(mem_only) return FALSE;
       if(mode->type==X86MT_IMM)
-        if(I8_MIN<=mode->imm<=I8_MAX)
+        if(I8_MIN<=mode->imm<=U8_MAX)
 	  goto pass;
       return FALSE;
     case X86_OPERAND_IMM16:
       if(mem_only) return FALSE;
       if(mode->type==X86MT_IMM)
-        if(I16_MIN<=mode->imm<=I16_MAX)
+        if(I16_MIN<=mode->imm<=U16_MAX)
 	  goto pass;
       return FALSE;
     case X86_OPERAND_IMM32:
       if(mem_only) return FALSE;
       if(mode->type==X86MT_IMM)
-        if(I32_MIN<=mode->imm<=I32_MAX)
+        if(mode->imm>=I32_MIN&&mode->imm<=U32_MAX)
 	  goto pass;
       return FALSE;
     case X86_OPERAND_IMM64:
@@ -371,6 +421,12 @@
 	      &&mode->r==md.u8[1])
 	  goto pass;
       return FALSE;
+    case X86_OPERAND_CONST_SREG:
+      if(mem_only) return FALSE;
+      if(mode->type==X86MT_REG)
+        if(mode->reg_type==REGT_SEG&&mode->r==md.u8[1])
+          goto pass;
+      return FALSE;
     case X86_OPERAND_Rx:
 rx:
       if(mem_only) return FALSE;
@@ -408,10 +464,10 @@
         switch(mem_sz) {
 	  case 8:
 	  case 4:
-	    if(I32_MIN<=mode->imm<=I32_MAX)
+	    if(I32_MIN<=mode->imm<=U32_MAX)
 	      goto pass;
 	  case 2:
-	    if(I16_MIN<=mode->imm<=I16_MAX)
+	    if(I16_MIN<=mode->imm<=U16_MAX)
 	      goto pass;
         } 
       }
@@ -474,10 +530,8 @@
 	  goto pass;
       return FALSE;
     case X86_OPERAND_REL8:
-/*
       if(mode->type==X86MT_IMM)
-        goto pass;
-*/
+	  goto pass;
       return FALSE;
     case X86_OPERAND_REL32:
       if(mode->type==X86MT_IMM)
@@ -713,12 +767,12 @@
   if(_disp_sz) *_disp_sz=disp_sz;
   return 1+(sib!=-1)+disp_sz;
 }
-I64 X86EncodeOpcode(I64 rip,CX86Opcode *opc,CX86AddrMode **modes,U8 *to,CQue *asm_patches) {
+I64 X86EncodeOpcode(I64 rip,I64 seg_size,CX86Opcode *opc,CX86AddrMode **modes,U8 *to,CQue *asm_patches) {
   U8 dummy[STR_LEN];
   I64 segment_override=-1;
   I64 i,ext=-1,a;
   I64 operand_sz=-1,len=0,sz,indir_mode_idx=-1;
-  I64 mask=1<<opc->arity-1;
+  I64 mask=(1<<opc->arity)-1;
   Bool is_x_sz=FALSE,wants_modrm_sib=FALSE;
   I64 rex=0,rel_sz=0; //Upper 4 bits will be 0x4 if needed
   if(!to) to=dummy;
@@ -731,7 +785,10 @@
         rel_sz=1;
         break;
       case X86_OPERAND_REL32:
-        rel_sz=4;
+        if(seg_size==16)
+          rel_sz=2;
+        else
+          rel_sz=4;
         break;
     }
 //Check for MOFFx/8
@@ -748,7 +805,7 @@
   for(a=0;a!=opc->arity;a++)
     if(modes[a]->type==X86MT_INDIR)
       if(-1!=modes[a]->seg) {
-        switch(modes[a]->r) {
+        switch(modes[a]->seg) {
 	  case 0: //ES
 	    segment_override=0x26;
 	    break;
@@ -764,7 +821,7 @@
 	  case 4: //FS
 	    segment_override=0x64;
 	    break;
-	  case 6: //FS
+	  case 5: //GS
 	    segment_override=0x65;
 	    break;
 	  default:
@@ -800,7 +857,7 @@
 	  for(a=0;a!=opc->arity;a++)
 	    if(modes[a]->type==X86MT_IMM&&operand_sz==-1)
 	      if(opc->operands[i].u8[0]==X86_OPERAND_IMMx) {
-	        if(I16_MIN<=modes[a]->imm<=I16_MAX&&!modes[a]->machine_code)
+	        if(I16_MIN<=modes[a]->imm<=U16_MAX&&!modes[a]->machine_code)
 		  operand_sz=2;
 	        else
 		  operand_sz=4;
@@ -810,7 +867,7 @@
         is_x_sz=TRUE;
     }
   if(operand_sz==-1&&opc->operands[0].u8[0]) //Make sure our opcode takes arguments
-    operand_sz=8;
+    operand_sz=seg_size/8;
 
   if(X86_OPCF_MODRM&opc->flags) {
     for(a=0,i=0;i!=4;i++)
@@ -946,7 +1003,12 @@
 }
 */
 
+  if(seg_size!=64)
+    rex=0;
+
   //Prefix group 2
+  if(opc->flags&X86_OPCF_REP)
+    to[len++]=0xf3;
   if(segment_override!=-1)
     to[len++]=segment_override;
 
@@ -954,7 +1016,9 @@
     to[len++]=opc->pf;  
   }
 //Prefix group 3(Operand size prefix)
-  if(is_x_sz&&operand_sz==2) {
+  if(is_x_sz&&(seg_size==16&&operand_sz==4||seg_size!=16&&operand_sz==2)||
+        opc->flags&X86_OPCF_OPSIZE16&&seg_size!=16||
+        opc->flags&X86_OPCF_OPSIZE32&&seg_size==16) {
     to[len++]=0x66;
   }
 //Prefix group 4(Address size ovveride)
@@ -1123,10 +1190,11 @@
 class CToFill:CQue {
   CHashExport *h;
 };
-U0 AsmResolvePatches(CCmpCtrl *cc,CQue *patches) {
+U0 AsmResolvePatches(CCmpCtrl *cc,CQue *patches,CQue *to_fill) {
   CAsmPatch *patch;
   I64 b,i;
+  CToFill *tmp_to_fill;
   CAsmUndefHash *tab,*cur;
   CHashImport *h;
   CMemberLst *mlst;
   CHashExport *exp;
@@ -1133,5 +1201,6 @@
   CCodeMisc *cm;
   Bool local;
+  I64 (*machine_code)();
   patch=patches->next;
   while(patch!=patches) {
     if(!patch->imm) goto nxt;
@@ -1145,10 +1215,23 @@
     if(!tab) goto nxt;
     cur=tab;
     while(cur) {
+      for(tmp_to_fill=to_fill->next;tmp_to_fill!=to_fill;
+            tmp_to_fill=tmp_to_fill->next) {
+        if(!StrCmp(tmp_to_fill->h->str,cur->hash->str)) {
+          exp=CAlloc(sizeof CHashExport);
+          exp->type=HTT_EXPORT_SYS_SYM|HTF_RESOLVE;
+          exp->str=StrNew(tmp_to_fill->h->str);
+          exp->val=tmp_to_fill->h->val;
+          HashAdd(exp,Fs->hash_table);
+          SysSymImportsResolve(cur->hash->str,0);
+          HashRemDel(exp,Fs->hash_table);
+          goto resolved;
+        }
+      }
       if(cc->coc.coc_next_misc)
         for(cm=cc->coc.coc_next_misc;cm!=&cc->coc.coc_next_misc;cm=cm->next) {
 	  if(cm->addr2!=INVALID_PTR&&
-	        cm->type==CMT_GOTO_LABEL&&cm->str&&
+	        (cm->type==CMT_GOTO_LABEL||cm->type==CMT_ASM_LABEL)&&cm->str&&
 	        !StrCmp(cm->str,cur->hash->str)) {
 	    exp=CAlloc(sizeof CHashExport);
 	    exp->type=HTT_EXPORT_SYS_SYM|HTF_RESOLVE;
@@ -1182,7 +1265,8 @@
 resolved:
       cur=cur->next;
     }
-    patch->imm->off=(*patch->imm->machine_code)();
+    machine_code=patch->imm->machine_code;
+    patch->imm->off=(*machine_code)();
     Free(patch->imm->machine_code);
     patch->imm->machine_code=NULL;
     LinkedLstDel(tab);
@@ -1201,14 +1285,36 @@
     tmp_to_fill->h->val+=buf;
     tmp_to_fill=tmp_to_fill->next;
   }
-  AsmResolvePatches(cc,blob->asm_patches);
-//If our relocations are not realtive,add the ptr to result
   patch=blob->asm_patches->next;
   if(cc->flags&CCF_AOT_COMPILE) {
-    if(patch!=blob->asm_patches)
-      LexExcept(cc,"AOT assembler not implemented yet for relocs");
+    while(patch!=blob->asm_patches) {
+      next_patch=patch->next;
+      if(patch->imm&&(patch->imm->local_asm_undef_hash||
+            patch->imm->glbl_asm_undef_hash)) {
+        switch(patch->sz) {
+          break;case 1: sz=IET_REL_I8;
+          break;case 2: sz=IET_REL_I16;
+          break;case 4: sz=IET_REL_I32;
+          break;case 8: sz=IET_REL_I64;
+          default: LexExcept(cc,"Invalid AOT assembler relocation size");
+        }
+        AsmUnresolvedAdd(cc,patch->imm->machine_code,
+              sz+!patch->is_rel,blob->final_start_rip+patch->rip,
+              blob->final_start_rip+patch->rip+patch->sz,
+              patch->imm->local_asm_undef_hash,
+              patch->imm->glbl_asm_undef_hash,
+              cc->lex_include_stk->line_num,FALSE);
+        patch->imm->machine_code=NULL;
+        patch->imm->local_asm_undef_hash=NULL;
+        patch->imm->glbl_asm_undef_hash=NULL;
+      }
+      patch=next_patch;
+    }
     return;
   }
+  AsmResolvePatches(cc,blob->asm_patches,blob->to_fill);
+//If our relocations are not realtive,add the ptr to result
+  patch=blob->asm_patches->next;
   while(patch!=blob->asm_patches) {
     next_patch=patch->next;
     if(patch->imm) {
@@ -1252,7 +1352,10 @@
   }
 }
 CAiwniosAOTBlob *PrsAsmBlk(CCmpCtrl *cc,I64 flags=0) {
-  Bool one_ins=FALSE;
+  I64 old_flags=cc->flags;
+  I64 old_class_dol_offset=cc->class_dol_offset;
+  CHashGeneric *asm_entry;
+  Bool one_ins=FALSE,has_zero_arity;
   if(flags&CMPF_ONE_ASM_INS) {
     one_ins=TRUE;
     goto enter0;
@@ -1273,7 +1376,7 @@
   I64 scope=0;
   CX86Opcode *opc,*first_opc,*best;
   CQue *asm_patches=MAlloc(sizeof CQue),*to_fill=MAlloc(sizeof CQue);
-  I64 arg_cnt,best_sz,sz,len=0,tmp,idx;
+  I64 arg_cnt,max_arity,best_sz,sz,len=0,tmp,idx,seg_size=64;
   U8 *buf=MAlloc(64),dummy[STR_LEN],*fbuf;
   CCodeMisc *cm;
   CHashExport *exp;
@@ -1284,17 +1387,35 @@
   QueInit(asm_patches);
   QueInit(to_fill);
 enter:
+  cc->flags|=CCF_CLASS_DOL_OFFSET;
+  cc->class_dol_offset=len;
+  if(cc->token==TK_IDENT) {
+    asm_entry=cc->hash_entry;
+    if(!asm_entry||!(asm_entry->type&(HTT_OPCODE|HTT_ASM_KEYWORD)))
+      asm_entry=HashFind(cc->cur_str,cmp.asm_hash,
+            HTT_OPCODE|HTT_ASM_KEYWORD);
+    if(asm_entry) cc->hash_entry=asm_entry;
+  }
   MemSetU64(modes,NULL,4);
-  if(cc->hash_entry&&cc->hash_entry->type&HTT_OPCODE) {
+  if(cc->token==TK_IDENT&&cc->hash_entry&&cc->hash_entry->type&HTT_OPCODE) {
     first_opc=cc->hash_entry;
+    max_arity=0;
+    has_zero_arity=FALSE;
+    for(opc=first_opc;opc;opc=opc->next)
+      if(!StrCmp(first_opc->str,opc->str)) {
+        if(opc->arity>max_arity)
+          max_arity=opc->arity;
+        if(!opc->arity)
+          has_zero_arity=TRUE;
+      }
     Lex(cc);
-    for(arg_cnt=0;arg_cnt!=first_opc->arity;arg_cnt++) {
+    if(has_zero_arity && cc->hash_entry &&
+        cc->hash_entry->type&(HTT_OPCODE|HTT_ASM_KEYWORD|HTT_KEYWORD))
+      max_arity=0;
+    for(arg_cnt=0;arg_cnt<max_arity;arg_cnt++) {
       if(arg_cnt) {
-        if(cc->token!=',') {
-	  LexExcept(cc,"Expected ',' at");
-	  Free(buf);
-	  throw('Asm');
-        }
+	if(cc->token!=',')
+	  break;
         Lex(cc);
       }
       modes[arg_cnt]=PrsX86AddrMode(cc);
@@ -1309,8 +1432,8 @@
     best=NULL,best_sz=I64_MAX;
     for(opc=first_opc;opc;opc=opc->next) {
       if(!StrCmp(first_opc->str,opc->str))
-       if(OpcodePassesModes(opc,modes)) {
-	  sz=X86EncodeOpcode(len,opc,modes,NULL,NULL);
+       if(opc->arity==arg_cnt && OpcodePassesModes(opc,modes)) {
+	  sz=X86EncodeOpcode(len,seg_size,opc,modes,NULL,NULL);
 	  if(sz<best_sz) {
 	    best_sz=sz;
 	    best=opc;
@@ -1318,14 +1441,28 @@
         }
     }
     if(!best) {
+      Print("MOUNTIN ASM DEBUG %s argc=%d\\n",first_opc->str,arg_cnt);
+      for(tmp=0;tmp<arg_cnt;tmp++)
+        Print("  arg%d type=%d reg_type=%d operand_sz=%d imm=%X machine=%X\\n",
+              tmp,modes[tmp]->type,modes[tmp]->reg_type,
+              modes[tmp]->operand_sz,modes[tmp]->imm,modes[tmp]->machine_code);
       LexExcept(cc,"Invalid usage of opcode:");
       Free(buf);
       throw('Asm');
     }
-    sz=X86EncodeOpcode(len,best,modes,dummy,asm_patches);
+    sz=X86EncodeOpcode(len,seg_size,best,modes,dummy,asm_patches);
     AddBytesToBuf(&buf,&len,sz,dummy);
   } else if(cc->token==TK_IDENT&&cc->hash_entry&&cc->hash_entry->type&(HTT_ASM_KEYWORD|HTT_KEYWORD)) {
-    if(!StrCmp(cc->hash_entry->str,"IMPORT")) {
+    if(!StrCmp(cc->hash_entry->str,"USE16")) {
+      seg_size=16;
+      Lex(cc);
+    } else if(!StrCmp(cc->hash_entry->str,"USE32")) {
+      seg_size=32;
+      Lex(cc);
+    } else if(!StrCmp(cc->hash_entry->str,"USE64")) {
+      seg_size=64;
+      Lex(cc);
+    } else if(!StrCmp(cc->hash_entry->str,"IMPORT")) {
 //TODO
     } else if(!StrCmp(cc->hash_entry->str,"BINFILE")) {
       if(Lex(cc)!=TK_STR)
@@ -1335,6 +1470,28 @@
       fbuf=FileRead(cc->cur_str,&sz);
       AddBytesToBuf(&buf,&len,sz,fbuf);
       Free(fbuf);
+    } else if(!StrCmp(cc->hash_entry->str,"ALIGN")) {
+      Lex(cc);
+      tmp_mode=CAlloc(sizeof CX86AddrMode);
+      tmp_mode->type=X86MT_IMM;
+      PrsAsmImm(cc,tmp_mode);
+      if(tmp_mode->local_asm_undef_hash||tmp_mode->glbl_asm_undef_hash||
+            tmp_mode->imm<=0||(tmp_mode->imm&(tmp_mode->imm-1)))
+        LexExcept(cc,"ALIGN must be a resolved power of two.");
+      arg_cnt=tmp_mode->imm;
+      X86AddrModeDel(tmp_mode);
+      if(cc->token!=',')
+        LexExcept(cc,"Expected ',' after ALIGN boundary.");
+      Lex(cc);
+      tmp_mode=CAlloc(sizeof CX86AddrMode);
+      tmp_mode->type=X86MT_IMM;
+      PrsAsmImm(cc,tmp_mode);
+      if(tmp_mode->local_asm_undef_hash||tmp_mode->glbl_asm_undef_hash)
+        LexExcept(cc,"ALIGN fill byte must be resolved.");
+      tmp=tmp_mode->imm;
+      X86AddrModeDel(tmp_mode);
+      while(len&(arg_cnt-1))
+        AddBytesToBuf(&buf,&len,1,&tmp);
     } else if(!StrCmp(cc->hash_entry->str,"DU8")) {
       sz=1;
 duxx:
@@ -1350,6 +1507,30 @@
 	  tmp_mode=CAlloc(sizeof CX86AddrMode);
 	  tmp_mode->type=X86MT_IMM;
 	  PrsAsmImm(cc,tmp_mode);
+	  if(cc->token==TK_IDENT&&cc->hash_entry&&
+		!StrCmp(cc->hash_entry->str,"DUP")) {
+	    if(tmp_mode->local_asm_undef_hash||tmp_mode->glbl_asm_undef_hash)
+	      LexExcept(cc,"DUP count must be resolved.");
+	    arg_cnt=tmp_mode->imm;
+	    X86AddrModeDel(tmp_mode);
+	    if(arg_cnt<0)
+	      LexExcept(cc,"DUP count must not be negative.");
+	    if(Lex(cc)!='(')
+	      LexExcept(cc,"Expected a '('.");
+	    Lex(cc);
+	    tmp_mode=CAlloc(sizeof CX86AddrMode);
+	    tmp_mode->type=X86MT_IMM;
+	    PrsAsmImm(cc,tmp_mode);
+	    if(tmp_mode->local_asm_undef_hash||tmp_mode->glbl_asm_undef_hash)
+	      LexExcept(cc,"DUP value must be resolved.");
+	    if(cc->token!=')')
+	      LexExcept(cc,"Expected a ')'.");
+	    Lex(cc);
+	    for(idx=0;idx<arg_cnt;idx++)
+	      AddBytesToBuf(&buf,&len,sz,&tmp_mode->imm);
+	    X86AddrModeDel(tmp_mode);
+	    goto duxx_next;
+	  }
 	  patch=CAlloc(sizeof CAsmPatch);
 	  patch->imm=tmp_mode;
 	  patch->rip=len;
@@ -1357,7 +1538,8 @@
 	  QueIns(patch,asm_patches);
 	  tmp=0;
 	  AddBytesToBuf(&buf,&len,sz,&tmp);
-        }
+	}
+duxx_next:
         if(cc->token==',') {
 	  Lex(cc);
         } else if(cc->token==';') {
@@ -1397,7 +1579,6 @@
         default:
 	  LexExcept(cc,"Expected a ':'");
       end:;
-        Lex(cc);
         if(StrNCmp("@@",dummy,2)) {
 	  cc->asm_local_scope++;
         } else {
@@ -1406,6 +1587,7 @@
 	  Free(tmp);
         }
         HashAdd(exp,cc->htc.glbl_hash_table);
+        Lex(cc);
     }
   } else if(cc->token==';') {
     Lex(cc);
@@ -1423,12 +1605,8 @@
   if(!one_ins) {
     if(cc->token!='}')
       goto enter;
-    if(Lex(cc)!=';') {
+    if(Lex(cc)==';')
       Lex(cc);
-      QueDel(to_fill);
-      Free(buf);
-      LexExcept(cc,"Expected a ';'.");
-    }
   }
 fin:
   ret_blob=CAlloc(sizeof CAiwniosAOTBlob);
@@ -1438,6 +1616,8 @@
   Free(asm_patches);
   ret_blob->len=len;  
   ret_blob->data=buf;
+  cc->flags=old_flags;
+  cc->class_dol_offset=old_class_dol_offset;
   return ret_blob;
 }
 U8 *AsmTest() {
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/CInit.HC b/Src/CInit.HC
--- a/Src/CInit.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/CInit.HC	2026-09-02 01:11:38.475182053 +0100
@@ -5,9 +5,12 @@
   {RT_I0,0,"I0i"},{RT_I0,0,"I0"},{RT_U0,0,"U0i"},{RT_U0,0,"U0"},
   {RT_I8,1,"I8i"},{RT_I8,1,"I8"},{RT_I8,1,"Bool"},
   {RT_U8,1,"U8i"},{RT_U8,1,"U8"},
-  {RT_I16,2,"I16i"},{RT_U16,2,"U16i"},
-  {RT_I32,4,"I32i"},{RT_U32,4,"U32i"},
-  {RT_I64,8,"I64i"},{RT_U64,8,"U64i"},
+  {RT_I16,2,"I16i"},{RT_I16,2,"I16"},
+  {RT_U16,2,"U16i"},{RT_U16,2,"U16"},
+  {RT_I32,4,"I32i"},{RT_I32,4,"I32"},
+  {RT_U32,4,"U32i"},{RT_U32,4,"U32"},
+  {RT_I64,8,"I64i"},{RT_I64,8,"I64"},
+  {RT_U64,8,"U64i"},{RT_U64,8,"U64"},
   {RT_F64,8,"F64i"},{RT_F64,8,"F64"},
 };
 
@@ -214,6 +217,14 @@
   HashAdd(gen,t);
 }
 
+U0 CmpAddAsmKw(CHashTable *t,U8 *str,I64 tok) {
+  CHashGeneric *gen=CAlloc(sizeof CHashGeneric);
+  gen->str=StrNew(str);
+  gen->type=HTT_ASM_KEYWORD;
+  gen->user_data0=tok;
+  HashAdd(gen,t);
+}
+
 U0 CmpLoadDefines()
 {
   DefineLstLoad("ST_RAW_TYPES",
@@ -419,9 +430,16 @@
   CmpAddKw(cmp.asm_hash,"haserrcode",45);
   CmpAddKw(cmp.asm_hash,"argpop",46);
   CmpAddKw(cmp.asm_hash,"noargpop",47);
-  CmpAddKw(cmp.asm_hash,"DU8",48);
-  CmpAddKw(cmp.asm_hash,"DU16",49);
-  CmpAddKw(cmp.asm_hash,"DU32",50);
-  CmpAddKw(cmp.asm_hash,"DU64",51);
+  CmpAddAsmKw(cmp.asm_hash,"DU8",48);
+  CmpAddAsmKw(cmp.asm_hash,"DU16",49);
+  CmpAddAsmKw(cmp.asm_hash,"DU32",50);
+  CmpAddAsmKw(cmp.asm_hash,"DU64",51);
+  CmpAddAsmKw(cmp.asm_hash,"DUP",55);
+  CmpAddAsmKw(cmp.asm_hash,"USE16",52);
+  CmpAddAsmKw(cmp.asm_hash,"USE32",53);
+  CmpAddAsmKw(cmp.asm_hash,"USE64",54);
+  CmpAddAsmKw(cmp.asm_hash,"ALIGN",56);
+  CmpAddAsmKw(cmp.asm_hash,"IMPORT",27);
+  CmpAddAsmKw(cmp.asm_hash,"BINFILE",57);
   adam_task->hash_table->next=cmp.asm_hash;
 }
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/DskAddDev.HC b/Src/DskAddDev.HC
--- a/Src/DskAddDev.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/DskAddDev.HC	2026-09-02 00:02:45.000000000 +0100
@@ -201,6 +201,8 @@
   }
   bd=BlkDevNextFreeSlot('T',BDT_VIRT);bd->unit=0;
   BlkDevAdd(bd,0,0,0);
+  bd=BlkDevNextFreeSlot('U',BDT_VIRT);bd->unit=0;
+  BlkDevAdd(bd,0,0,0);
   Fs->cur_dv=Let2Drv('T');
   Fs->cur_dir=StrNew("/");
   blkdev.boot_drv_let='T';
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/KLoadARM.HC b/Src/KLoadARM.HC
--- a/Src/KLoadARM.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/KLoadARM.HC	2026-09-02 00:02:45.000000000 +0100
@@ -113,7 +113,7 @@
 U0 LoadPass1(U8 *src,U8 *module_base,I64 ld_flags)
 {
   U8 *ptr2,*ptr3,*st_ptr;
-  I64 i,j,cnt,etype;
+  I64 i,j,cnt,etype,ref_offset;
   CHashExport *tmpex=NULL;
   while (etype=*src++) {
     i=*src(U32 *)++;
@@ -144,7 +144,7 @@
 	else {
 	  cnt=i;
 	  for (j=0;j<cnt;j++) {
-	    ptr2=module_base+*src(U64 *)++;
+	    ptr2=module_base+*src(U32 *)++;
 	    *ptr2(U64 *)+=module_base;
 	  }
 	}
@@ -190,13 +190,35 @@
 	cnt=i;
 	for (j=0;j<cnt;j++) {
 	  ptr2=module_base+*src(U32 *)++;
-	  *ptr2(I64 *)+=ptr3;
+	  ref_offset=*src(U32 *)++;
+	  *ptr2(I64 *)+=ptr3+ref_offset;
 	}
 	break;
     }
   }
 }
 
+Bool BinSignatureValid(U32 signature)
+{
+  if (signature==BIN_SIGNATURE_VAL)
+    return TRUE;
+#ifdef TARGET_BYTECODE
+  return signature=='BYTC';
+#endif
+#ifdef TARGET_X86
+  if (HOST_ABI=='OpenBSD')
+    return signature=='OX86';
+  return signature=='X86';
+#endif
+#ifdef TARGET_AARCH64
+  return signature=='ARM';
+#endif
+#ifdef TARGET_RISCV
+  return signature=='RV64';
+#endif
+  return FALSE;
+}
+
 U0 LoadPass2(U8 *src,U8 *module_base,I64)
 {
   U8 *st_ptr;
@@ -249,7 +271,7 @@
 
   //See $$LK,"Patch Table Generation",A="FF:::/Compiler/CMain.HC,IET_ABS_ADDR"$$
   module_align=1<<bfh->module_align_bits;
-  if (!module_align || bfh->bin_signature!=BIN_SIGNATURE_VAL) {
+  if (!module_align || !BinSignatureValid(bfh->bin_signature)) {
     Free(bfh);
     Free(fbuf);
     throw('BinModul');
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/KernelA.HH b/Src/KernelA.HH
--- a/Src/KernelA.HH	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/KernelA.HH	2026-09-02 00:02:45.000000000 +0100
@@ -6916,12 +6916,16 @@
   I64 len;
   U8 *data;
   //private,used when placing ->data into it's final location
-  U8 **final_start_addr;
+  I64 final_start_rip;
+  U8 *final_start_addr;
 };
 extern U0 AiwniosAOTBlobDel(CAiwniosAOTBlob *b);
+extern U0 AsmUnresolvedAdd(CCmpCtrl *cc,U8 *machine_code,I64 type,I64 rip,
+      I64 rel_rip,CAsmUndefHash *local_asm_undef_hash,
+      CAsmUndefHash *glbl_asm_undef_hash,I64 line_num,Bool U8_avail);
 extern U0 JITFixupAiwniosAOTBlob(CCmpCtrl *cc,CAiwniosAOTBlob *blob,U8 *buf);
 extern U0 AsmResolvePatches(CCmpCtrl *cc,CQue *patches);
-extern I64 X86EncodeOpcode(I64 rip,U8 *opc,U8 **modes,U8 *to,CQue *asm_patches);
+extern I64 X86EncodeOpcode(I64 rip,I64 seg_size,U8 *opc,U8 **modes,U8 *to,CQue *asm_patches);
 #ifdef TARGET_X86
 extern U8 SSE_ShuffleByte(I64 a,I64 b,I64 c,I64 d);
 #endif
@@ -6929,7 +6933,7 @@
 #define REGISTRY_FILENAME "~/Registry.HC.Z"
 #define SE_NOISE	0
 #define SE_SWEEP	1
-#define INTERNAL_TYPES_NUM	17
+#define INTERNAL_TYPES_NUM	23
 #define WIN_SCROLL_SIZE		8
 #define WIN_SCROLL_BORDER_BONUS	4
 #define VIEWANGLES_SPACING	22
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/Lex.HC b/Src/Lex.HC
--- a/Src/Lex.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/Lex.HC	2026-09-02 00:02:45.000000000 +0100
@@ -701,6 +701,14 @@
 	      goto lex_end;
 	    fbuf=ExtDft(cc->cur_str,"HC.Z");
 	    buf2=FileNameAbs(fbuf);
+	    if (!FileFind(buf2) && *fbuf!='/' && !StrOcc(fbuf,':') &&
+		cc->lex_include_stk && cc->lex_include_stk->full_name) {
+	      buf3=StrNew(cc->lex_include_stk->full_name);
+	      StrLastRem(buf3,"/");
+	      Free(buf2);
+	      buf2=MStrPrint("%s/%s",buf3,fbuf);
+	      Free(buf3);
+	    }
 	    Free(fbuf);
 	    if (!TEXT_MODE) //TODO Was 1  TODO RESTORE 
 	      LexAttachDoc(cc,,,buf2);
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/Src/UAsmX64.HC b/Src/UAsmX64.HC
--- a/Src/UAsmX64.HC	2026-02-02 11:57:50.000000000 +0000
+++ b/Src/UAsmX64.HC	2026-09-02 00:52:35.411037755 +0100
@@ -5,6 +5,9 @@
 #define X86_OPCF_ADD_REG 0b10
 #define X86_OPCF_REXW 0b100
 #define X86_OPCF_REX 0b1000
+#define X86_OPCF_REP 0b10000
+#define X86_OPCF_OPSIZE16 0b100000
+#define X86_OPCF_OPSIZE32 0b1000000
 
 #define X86_OPERAND_R8 1
 #define X86_OPERAND_RM8 2
@@ -55,6 +58,7 @@
 #define X86_OPERAND_RMMM64 48
 //TODO,Nroot is not implementeing vector types now.
 #define X86_OPERAND_M128 49
+#define X86_OPERAND_CONST_SREG 50
 
 class CX86Opcode:CHash {
   //-1 for absent
@@ -117,6 +121,7 @@
         case X86_OPERAND_CONST_R32:
         case X86_OPERAND_CONST_R64:
         case X86_OPERAND_CONST_Rx:
+        case X86_OPERAND_CONST_SREG:
         case X86_OPERAND_CONST_IMM:
         case X86_OPERAND_EXT:
 	  opc->operands[op]|=argv[idx+1]<<8;
@@ -135,12 +140,6 @@
     *StrLastOcc(name,",")=0;
   for(op=0;idx!=op;op++) {
     test=HashFind(LstSub(op,name),cmp.asm_hash,HTT_OPCODE);
-    if(test) {
-      if(test->arity!=opc->arity) {
-	DbgPrint("ARITYMISSAMATCH:%s\n",LstSub(op,name));
-	throw('AsmItrn');
-      }
-    }
     if(flags&X86_OPCF_ADD_REG) {
       idx2=8;
       while(--idx2>=0) {
@@ -1104,7 +1103,7 @@
   AO("BTC",-1,0xf,0xbb,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_Rx);
   AO("BTR",-1,0xf,0xb3,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_Rx);
   AO("BTR",-1,0xf,0xba,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_IMM8,X86_OPERAND_EXT,6);
-  AO("BTS",-1,0xf,0xab,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_IMM8);
+  AO("BTS",-1,0xf,0xab,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_Rx);
   AO("BTS",-1,0xf,0xba,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_IMM8,X86_OPERAND_EXT,5);
   
   AO("CALL",-1,-1,0xe8,-1,0,X86_OPERAND_REL32);
@@ -1275,9 +1274,9 @@
   AO("FISUB",-1,-1,0xde,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_M16,X86_OPERAND_EXT,4);
   AO("FISUBR",-1,-1,0xda,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_M32,X86_OPERAND_EXT,5);
   AO("FISUBR",-1,-1,0xde,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_M16,X86_OPERAND_EXT,5);
-  AO("FLD",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_FRM32,X86_OPERAND_EXT,0);
-//  AO("FLD",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_M80,X86_OPERAND_EXT,5);
-  AO("FLD",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_M64,X86_OPERAND_EXT,0);
+  AO("FLD",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_FRM32,X86_OPERAND_EXT,0);
+//  AO("FLD",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_M80,X86_OPERAND_EXT,5);
+  AO("FLD",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_EXT,0);
   AO("FLD1",-1,-1,0xd9,0xe8,0);
   AO("FLDCW",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_M16,X86_OPERAND_EXT,5);
   AO("FLDENV",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_M32,X86_OPERAND_EXT,4);
@@ -1309,17 +1308,17 @@
   AO("FSIN",-1,-1,0xd9,0xfe,0);
   AO("FSINCOS",-1,-1,0xd9,0xfb,0);
   AO("FSQRT",-1,-1,0xd9,0xfa,0);
-  AO("FST",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_M32,X86_OPERAND_ST0,X86_OPERAND_EXT,2);
-  AO("FST",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_ST0,X86_OPERAND_EXT,2);
-  AO("FST",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_STi,X86_OPERAND_EXT,2);
+  AO("FST",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_M32,X86_OPERAND_EXT,2);
+  AO("FST",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_EXT,2);
+  AO("FST",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_STi,X86_OPERAND_EXT,2);
   AO("FSTCW",0x9b,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_M16,X86_OPERAND_EXT,7);
   AO("FSTENV",0x9b,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_STi,X86_OPERAND_EXT,6);
-  AO("FSTP",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_M32,X86_OPERAND_ST0,X86_OPERAND_EXT,3);
+  AO("FSTP",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_M32,X86_OPERAND_EXT,3);
 //  AO("FSTP",-1,-1,0xdb,-1,X86_OPCF_MODRM,X86_OPERAND_M80,X86_OPERAND_ST0,X86_OPERAND_EXT,7);
-  AO("FSTP",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_ST0,X86_OPERAND_EXT,3);
-  AO("FSTP",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_STi,X86_OPERAND_EXT,3);
+  AO("FSTP",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_EXT,3);
+  AO("FSTP",-1,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_STi,X86_OPERAND_EXT,3);
   AO("FSTSW",0x9b,-1,0xdd,-1,X86_OPCF_MODRM,X86_OPERAND_M16,X86_OPERAND_EXT,7);
-  AO("FSTSW",0x9b,-1,0xdf,0xe0,0,X86_OPERAND_CONST_R16,REG_RAX);
+  AO("FSTSW",0x9b,-1,0xdf,0xe0,0);
   AO("FSUB",-1,-1,0xd8,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_M32,X86_OPERAND_EXT,4);
   AO("FSUB",-1,-1,0xd8,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_STi,X86_OPERAND_EXT,4);
   AO("FSUB",-1,-1,0xdc,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_M64,X86_OPERAND_EXT,4);
@@ -1338,8 +1337,8 @@
   AO("FUCOMPP",-1,-1,0xde,0xd9,0);
   AO("FXAM",-1,-1,0xd9,0xe5,0);
   AO("FXCH",-1,-1,0xd9,-1,X86_OPCF_MODRM,X86_OPERAND_ST0,X86_OPERAND_STi,X86_OPERAND_EXT,1);
-  AO("FXRSTOR",-1,0xf,0xae,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_EXT,1);
-  AO("FXSAVE",-1,0xf,0xae,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_EXT,0);
+  AO("FXRSTOR",-1,0xf,0xae,-1,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_EXT,1);
+  AO("FXSAVE",-1,0xf,0xae,-1,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_EXT,0);
   AO("FXTRACT",-1,-1,0xd9,0xf4,0);
   AO("FYL2X",-1,-1,0xd9,0xf1,0);
   AO("FYL2XP1",-1,-1,0xd9,0xf9,0);
@@ -1428,7 +1427,7 @@
   AO("LEAVE",-1,-1,0xc9,-1,0);
   AO("MFENCE",-1,0xf,0xae,0xf0,0);
   AO("LFS",-1,-1,0x0f,0xb4,X86_OPCF_MODRM,X86_OPERAND_Rx,X86_OPERAND_RMx);
-  AO("LGDT",-1,-1,0x0f,0x01,X86_OPCF_MODRM,X86_OPERAND_M64,X86_OPERAND_EXT,2);
+  AO("LGDT",-1,-1,0x0f,0x01,X86_OPCF_MODRM,X86_OPERAND_Mx,X86_OPERAND_EXT,2);
   AO("LGS",-1,-1,0x0f,0xb5,X86_OPCF_MODRM,X86_OPERAND_Rx,X86_OPERAND_RMx);
   AO("LIDT",-1,-1,0x0f,0x01,X86_OPCF_MODRM,X86_OPERAND_Mx,X86_OPERAND_EXT,3);
   AO("LLDT",-1,-1,0x0f,0x00,X86_OPCF_MODRM,X86_OPERAND_Mx,X86_OPERAND_EXT,2);
@@ -1496,6 +1495,21 @@
   AO("MOVSW",0x66,-1,0xa5,-1,0);
   AO("MOVSD",-1,-1,0xa5,-1,0);
   AO("MOVSQ",-1,-1,0xa5,-1,X86_OPCF_REXW);
+  AO("REP_MOVSB",-1,-1,0xa4,-1,X86_OPCF_REP);
+  AO("REP_MOVSW",-1,-1,0xa5,-1,X86_OPCF_REP|X86_OPCF_OPSIZE16);
+  AO("REP_MOVSD",-1,-1,0xa5,-1,X86_OPCF_REP|X86_OPCF_OPSIZE32);
+  AO("REP_MOVSQ",-1,-1,0xa5,-1,X86_OPCF_REP|X86_OPCF_REXW);
+  AO("REPE_CMPSB",-1,-1,0xa6,-1,X86_OPCF_REP);
+  AO("REP_INSB",-1,-1,0x6c,-1,X86_OPCF_REP);
+  AO("REP_INSW",-1,-1,0x6d,-1,X86_OPCF_REP|X86_OPCF_OPSIZE16);
+  AO("REP_INSD",-1,-1,0x6d,-1,X86_OPCF_REP|X86_OPCF_OPSIZE32);
+  AO("REP_OUTSB",-1,-1,0x6e,-1,X86_OPCF_REP);
+  AO("REP_OUTSW",-1,-1,0x6f,-1,X86_OPCF_REP|X86_OPCF_OPSIZE16);
+  AO("REP_OUTSD",-1,-1,0x6f,-1,X86_OPCF_REP|X86_OPCF_OPSIZE32);
+  AO("REP_STOSB",-1,-1,0xaa,-1,X86_OPCF_REP);
+  AO("REP_STOSW",-1,-1,0xab,-1,X86_OPCF_REP|X86_OPCF_OPSIZE16);
+  AO("REP_STOSD",-1,-1,0xab,-1,X86_OPCF_REP|X86_OPCF_OPSIZE32);
+  AO("REP_STOSQ",-1,-1,0xab,-1,X86_OPCF_REP|X86_OPCF_REXW);
   AO("MOVSS",0xf3,0xf,0x10,-1,X86_OPCF_MODRM,X86_OPERAND_RXMM,X86_OPERAND_RMXMM32);
   AO("MOVSS",0xf3,0xf,0x11,-1,X86_OPCF_MODRM,X86_OPERAND_RMXMM32	,X86_OPERAND_RXMM);
   AO("MOVSD2",0xf2,0xf,0x10,-1,X86_OPCF_MODRM,X86_OPERAND_RXMM,X86_OPERAND_RMXMM64);
@@ -1659,10 +1673,17 @@
 
   AO("POP",-1,-1,0x58,-1,X86_OPCF_ADD_REG,X86_OPERAND_R64);
   AO("POP",-1,-1,0x8f,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_EXT,0);
+  AO("POP",-1,-1,0x07,-1,0,X86_OPERAND_CONST_SREG,0);
+  AO("POP",-1,-1,0x17,-1,0,X86_OPERAND_CONST_SREG,2);
+  AO("POP",-1,-1,0x1f,-1,0,X86_OPERAND_CONST_SREG,3);
+  AO("POP",-1,0xf,0xa1,-1,0,X86_OPERAND_CONST_SREG,4);
+  AO("POP",-1,0xf,0xa9,-1,0,X86_OPERAND_CONST_SREG,5);
 //POP GS
 //POP FS
   AO("POPCNT",0xf3,0xf,0xb8,-1,X86_OPCF_MODRM,X86_OPERAND_Rx,X86_OPERAND_RMx);
   AO("POPF",-1,-1,0x9d,-1,0);
+  AO("POPFW,POPFD",-1,-1,0x9d,-1,0);
+  AO("POPAW,POPAD",-1,-1,0x61,-1,0);
   AO("POR",-1,0xf,0xeb,-1,X86_OPCF_MODRM,X86_OPERAND_RMM,X86_OPERAND_RMMM64);
   AO("POR",0x66,0xf,0xeb,-1,X86_OPCF_MODRM,X86_OPERAND_RXMM,X86_OPERAND_RMXMM);
 //  AO("PREFETCHNTA",-1,0xf,0x18,-1,X86_OPCF_MODRM,X86_OPERAND_M8,X86_OPERAND_EXT,0);
@@ -1743,9 +1764,17 @@
   AO("PUSH",-1,-1,0x68,-1,0,X86_OPERAND_IMM32);
   AO("PUSH",-1,-1,0x6a,-1,0,X86_OPERAND_IMM8);
   AO("PUSH",-1,-1,0xff,-1,X86_OPCF_MODRM,X86_OPERAND_EXT,6,X86_OPERAND_RMx);
+  AO("PUSH",-1,-1,0x06,-1,0,X86_OPERAND_CONST_SREG,0);
+  AO("PUSH",-1,-1,0x0e,-1,0,X86_OPERAND_CONST_SREG,1);
+  AO("PUSH",-1,-1,0x16,-1,0,X86_OPERAND_CONST_SREG,2);
+  AO("PUSH",-1,-1,0x1e,-1,0,X86_OPERAND_CONST_SREG,3);
+  AO("PUSH",-1,0xf,0xa0,-1,0,X86_OPERAND_CONST_SREG,4);
+  AO("PUSH",-1,0xf,0xa8,-1,0,X86_OPERAND_CONST_SREG,5);
 //PUSH FS
 //PUSH Gs
   AO("PUSHFQ,PUSHF",-1,-1,0x9c,-1,0);
+  AO("PUSHFW,PUSHFD",-1,-1,0x9c,-1,0);
+  AO("PUSHAW,PUSHAD",-1,-1,0x60,-1,0);
 
   AO("PXOR",-1,0xf,0xef,-1,X86_OPCF_MODRM,X86_OPERAND_RMM,X86_OPERAND_RMMM64);
   AO("PXOR",0x66,0xf,0xef,-1,X86_OPCF_MODRM,X86_OPERAND_RXMM,X86_OPERAND_RMXMM128);
@@ -1853,6 +1882,8 @@
   AO("SHL,SAL",-1,-1,0xc1,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_IMM8,X86_OPERAND_EXT,4);
   AO("SHL,SAL",-1,-1,0xd0,-1,X86_OPCF_MODRM,X86_OPERAND_RM8,X86_OPERAND_CONST_IMM,1,X86_OPERAND_EXT,4);
   AO("SHL,SAL",-1,-1,0xd1,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_CONST_IMM,1,X86_OPERAND_EXT,4);
+  AO("SHL1",-1,-1,0xd0,-1,X86_OPCF_MODRM,X86_OPERAND_RM8,X86_OPERAND_EXT,4);
+  AO("SHL1",-1,-1,0xd1,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_EXT,4);
   AO("SHL,SAL",-1,-1,0xd2,-1,X86_OPCF_MODRM,X86_OPERAND_RM8,X86_OPERAND_CONST_R8,REG_RCX,X86_OPERAND_EXT,4);
   AO("SHL,SAL",-1,-1,0xd3,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_CONST_R8,REG_RCX,X86_OPERAND_EXT,4);
 
@@ -1868,6 +1899,7 @@
 
   AO("SHRD",-1,0xf,0xac,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_Rx,X86_OPERAND_IMM8);
   AO("SHRD",-1,0xf,0xad,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_Rx,X86_OPERAND_CONST_R8,REG_RCX);
+  AO("SHRD",-1,0xf,0xad,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_Rx);
 
   AO("SHUFPD",0x66,0xF,0xc6,-1,X86_OPCF_MODRM,X86_OPERAND_RXMM,X86_OPERAND_RMXMM128,X86_OPERAND_IMM8);
   AO("SHUFPS",-1,0xF,0xc6,-1,X86_OPCF_MODRM,X86_OPERAND_RXMM,X86_OPERAND_RMXMM128,X86_OPERAND_IMM8);
@@ -1931,10 +1963,28 @@
   AO("UNPCKLPS",-1,0xF,0x14,-1,X86_OPCF_MODRM,X86_OPERAND_RXMM,X86_OPERAND_RMXMM64);
   AO("WBINVD",-1,0xF,0x09,-1,0);
   AO("WRMSR",-1,0xf,0x30,-1,0);
+  AO("MOV_CR0_EAX",-1,0x0f,0x22,0xc0,0);
+  AO("MOV_EAX_CR0",-1,0x0f,0x20,0xc0,0);
+  AO("MOV_CR2_EAX",-1,0x0f,0x22,0xd0,0);
+  AO("MOV_EAX_CR2",-1,0x0f,0x20,0xd0,0);
+  AO("MOV_CR3_EAX",-1,0x0f,0x22,0xd8,0);
+  AO("MOV_EAX_CR3",-1,0x0f,0x20,0xd8,0);
+  AO("MOV_CR4_EAX",-1,0x0f,0x22,0xe0,0);
+  AO("MOV_EAX_CR4",-1,0x0f,0x20,0xe0,0);
+  AO("MOV_CR0_RAX",-1,0x0f,0x22,0xc0,0);
+  AO("MOV_RAX_CR0",-1,0x0f,0x20,0xc0,0);
+  AO("MOV_CR2_RAX",-1,0x0f,0x22,0xd0,0);
+  AO("MOV_RAX_CR2",-1,0x0f,0x20,0xd0,0);
+  AO("MOV_CR3_RAX",-1,0x0f,0x22,0xd8,0);
+  AO("MOV_RAX_CR3",-1,0x0f,0x20,0xd8,0);
+  AO("MOV_CR4_RAX",-1,0x0f,0x22,0xe0,0);
+  AO("MOV_RAX_CR4",-1,0x0f,0x20,0xe0,0);
   AO("XADD",-1,0xf,0xc0,-1,X86_OPCF_MODRM,X86_OPERAND_RM8,X86_OPERAND_R8);
   AO("XADD",-1,0xf,0xc1,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_Rx);
   AO("XCHG",-1,-1,0x86,-1,X86_OPCF_MODRM,X86_OPERAND_R8,X86_OPERAND_RM8);
+  AO("XCHG",-1,-1,0x86,-1,X86_OPCF_MODRM,X86_OPERAND_RM8,X86_OPERAND_R8);
   AO("XCHG",-1,-1,0x87,-1,X86_OPCF_MODRM,X86_OPERAND_Rx,X86_OPERAND_RMx);
+  AO("XCHG",-1,-1,0x87,-1,X86_OPCF_MODRM,X86_OPERAND_RMx,X86_OPERAND_Rx);
   AO("XCHG",-1,-1,0x90,-1,X86_OPCF_ADD_REG,X86_OPERAND_Rx,X86_OPERAND_CONST_Rx,REG_RAX);
   AO("XGETBV",-1,0xF,0x1,0xD0,0);
   AO("XLAT,XLATB",-1,-1,0xd7,-1,0);
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/c/fs.c b/c/fs.c
--- a/c/fs.c	2026-02-02 11:57:50.000000000 +0000
+++ b/c/fs.c	2026-09-02 00:02:45.000000000 +0100
@@ -403,6 +403,7 @@
   int idx = toupper(let) - 'A';
   char *p = StPCpy(mount_points[idx], path);
   strcpy(p, "/");
+  return 1;
 }
 
 int64_t VFsFBlkRead(void *d, int64_t sz, int f) {
diff -ruN '--exclude=.git' '--exclude=build' '--exclude=cmake-build' '--exclude=aiwnios' '--exclude=*.orig' '--exclude=STAGE1.HC' '--exclude=*.BIN' '--exclude=*.BIN.Z' '--exclude=*.DBG.Z' '--exclude=Registry.HC.Z' a/c/main.c b/c/main.c
--- a/c/main.c	2026-02-02 11:57:50.000000000 +0000
+++ b/c/main.c	2026-09-02 00:02:45.000000000 +0100
@@ -72,7 +72,8 @@
 struct arg_lit *arg_help, *arg_overwrite, *arg_new_boot_dir, *arg_asan_enable,
     *sixty_fps, *arg_cmd_line, *arg_cmd_line2 = NULL, *arg_fork, *arg_no_debug,
                                *arg_grab, *arg_fast_fail, *arg_s;
-struct arg_file *arg_t_dir, *arg_bootstrap_bin, *arg_boot_files, *arg_pidfile;
+struct arg_file *arg_t_dir, *arg_host_dir, *arg_bootstrap_bin,
+    *arg_boot_files, *arg_pidfile;
 static struct arg_end *_arg_end;
 #ifdef AIWNIOS_TESTS
 // Import PrintI first
@@ -1941,6 +1942,8 @@
   }
   InstallDbgSignalsForThread();
   VFsMountDrive('T', t_drive);
+  if (arg_host_dir->count)
+    VFsMountDrive('U', (char *)arg_host_dir->filename[0]);
 #if defined(__EMSCRIPTEN__)
   EM_ASM(FS.mkdir('/persist'); FS.mount(IDBFS, {}, '/persist');
          FS.syncfs(true, function(err){}););
@@ -2075,6 +2078,9 @@
                    "Overwrite the T directory with the installed T template."),
       arg_t_dir = arg_file0("t", NULL, "Directory",
                             "Specify the boot drive(dft is current dir)."),
+      arg_host_dir =
+          arg_file0("U", "host-drive", "Directory",
+                    "Mount a host directory as the U drive."),
       arg_bootstrap_bin =
           arg_lit0("b", "bootstrap",
                    "Build a new binary with the \"slim\" compiler of aiwnios."),
