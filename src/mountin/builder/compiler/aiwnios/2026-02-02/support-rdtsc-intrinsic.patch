--- a/Src/AIWNIOS_CodeGen.HC
+++ b/Src/AIWNIOS_CodeGen.HC
@@ -93,6 +93,7 @@
 import U8 *__HC_ICAdd_Neg(U8*);
 import U8 *__HC_ICAdd_Bsf(U8*);
 import U8 *__HC_ICAdd_Bsr(U8*);
+import U8 *__HC_ICAdd_Rdtsc(U8*);
 import U8 *__HC_ICAdd_Ret(U8*);
 import U8 *__HC_ICAdd_Arg(U8*,I64);
 import U8 *__HC_ICAdd_SetFrameSize(U8*,I64);
@@ -197,6 +198,7 @@
 extern U8 *__HC_ICAdd_Neg(U8*);
 extern U8 *__HC_ICAdd_Bsf(U8*);
 extern U8 *__HC_ICAdd_Bsr(U8*);
+extern U8 *__HC_ICAdd_Rdtsc(U8*);
 extern U8 *__HC_ICAdd_Ret(U8*);
 extern U8 *__HC_ICAdd_Arg(U8*,I64);
 extern U8 *__HC_ICAdd_SetFrameSize(U8*,I64);
@@ -1968,6 +1970,9 @@
     new=__HC_ICAdd_Bsf(cc2);
     break;case IC_BSR:
     new=__HC_ICAdd_Bsr(cc2);
+    break;case IC_RDTSC:
+    "MOUNTIN: translating RDTSC intrinsic\n";
+    new=__HC_ICAdd_Rdtsc(cc2);
     break;case IC_SQRT:
     new=__HC_ICAdd_Sqrt(cc2);
     break;case IC_DEREF:
--- a/c/aiwn_lexparser.h
+++ b/c/aiwn_lexparser.h
@@ -240,6 +240,7 @@
   IC_NEG,
   IC_BSF,
   IC_BSR,
+  IC_RDTSC,
   IC_POS,
   IC_NAME,
   IC_STR, //->code_misc
@@ -685,6 +686,7 @@
 HC_IC_BINDINGH(HC_ICAdd_Neg)
 HC_IC_BINDINGH(HC_ICAdd_Bsf)
 HC_IC_BINDINGH(HC_ICAdd_Bsr)
+HC_IC_BINDINGH(HC_ICAdd_Rdtsc)
 HC_IC_BINDINGH(HC_ICAdd_And)
 HC_IC_BINDINGH(HC_ICAdd_EqEq)
 HC_IC_BINDINGH(HC_ICAdd_Ret)
--- a/c/main.c
+++ b/c/main.c
@@ -1196,6 +1196,9 @@
 static int64_t STK___HC_ICAdd_Bsr(int64_t *stk) {
   return __HC_ICAdd_Bsr(stk[0]);
 }
+static int64_t STK___HC_ICAdd_Rdtsc(int64_t *stk) {
+  return __HC_ICAdd_Rdtsc(stk[0]);
+}
 static int64_t STK___HC_ICAdd_Ret(int64_t *stk) {
   return __HC_ICAdd_Ret(stk[0]);
 }
@@ -1809,6 +1812,7 @@
     PrsAddSymbol("__HC_ICAdd_Neg", STK___HC_ICAdd_Neg, 1);
     PrsAddSymbol("__HC_ICAdd_Bsf", STK___HC_ICAdd_Bsf, 1);
     PrsAddSymbol("__HC_ICAdd_Bsr", STK___HC_ICAdd_Bsr, 1);
+    PrsAddSymbol("__HC_ICAdd_Rdtsc", STK___HC_ICAdd_Rdtsc, 1);
     PrsAddSymbol("__HC_ICAdd_Ret", STK___HC_ICAdd_Ret, 1);
     PrsAddSymbol("__HC_ICAdd_Arg", STK___HC_ICAdd_Arg, 2);
     PrsAddSymbol("__HC_ICAdd_SetFrameSize", STK___HC_ICAdd_SetFrameSize, 2);
--- a/c/parser.c
+++ b/c/parser.c
@@ -219,6 +219,7 @@
     goto unop;
   case IC_LOCK:
   case IC_CURRENT_RIP:
+  case IC_RDTSC:
   case IC_RAW_BYTES:
   case __IC_STATICS_SIZE:
   case __IC_SET_STATIC_DATA:
@@ -797,6 +798,10 @@
     printf("NOP\n");
     goto ret;
     break;
+  case IC_RDTSC:
+    printf("RDTSC\n");
+    goto ret;
+    break;
   case IC_NAME:
     printf("NAME:%s\n", rpn->string);
     goto ret;
@@ -1319,6 +1324,10 @@
   case IC_NOP:
     goto ret;
     break;
+  case IC_RDTSC:
+    len = AddBytes(to, len, "$PURPLE$RDTSC$FD$\n");
+    goto ret;
+    break;
   case IC_NAME:
     goto ret;
     break;
@@ -3459,6 +3468,7 @@
     break;
   case IC_FS:
   case IC_GS:
+  case IC_RDTSC:
     rpn->ic_class = HashFind("I64i", Fs->hash_table, HTT_CLASS, 1);
     return rpn->raw_type = RT_I64i;
     break;
@@ -4634,6 +4644,7 @@
 HC_IC_BINDING(HC_ICAdd_Neg, IC_NEG);
 HC_IC_BINDING(HC_ICAdd_Bsf, IC_BSF);
 HC_IC_BINDING(HC_ICAdd_Bsr, IC_BSR);
+HC_IC_BINDING(HC_ICAdd_Rdtsc, IC_RDTSC);
 HC_IC_BINDING(HC_ICAdd_And, IC_AND);
 HC_IC_BINDING(HC_ICAdd_Ret, IC_RET);
 
--- a/c/x86_64_backend.c
+++ b/c/x86_64_backend.c
@@ -1359,6 +1359,17 @@
   return len;
 }
 
+static int64_t X86Rdtsc(char *to, int64_t unused) {
+  int64_t len = 0;
+  char buf[16];
+  (void)unused;
+  if (!to)
+    to = buf;
+  ADD_U8(0x0f);
+  ADD_U8(0x31);
+  return len;
+}
+
 static int64_t X86IMul2SIB64(char *to, int64_t r, int64_t s, int64_t i,
                              int64_t b, int64_t off) {
   int64_t len = 0;
@@ -2187,6 +2197,8 @@
   if (rpn->flags & ICF_DOESNT_SPILL_TMP_REGS)
     return 0;
   switch (rpn->type) {
+  case IC_RDTSC:
+    goto pass;
   case IC_FS:
   case IC_GS:
     goto fail;
@@ -2834,6 +2846,7 @@
   case IC_FREG:
   case IC_BASE_PTR:
   case IC_CURRENT_RIP:
+  case IC_RDTSC:
   case IC_STR:
   case IC_CHR:
   case IC_FS:
@@ -6096,6 +6109,7 @@
   int64_t idx;
   CRPN *left, *right, *last;
   switch (rpn->type) {
+  case IC_RDTSC:
   case IC_FS:
   case IC_GS:
     return;
@@ -6354,12 +6368,22 @@
       [IC_TAN] = &&ic_tan,
       [IC_ATAN] = &&ic_atan,
       [IC_CURRENT_RIP] = &&ic_current_rip,
+      [IC_RDTSC] = &&ic_rdtsc,
       [IC_RAW_BYTES] = &&ic_raw_bytes,
   };
   if (!poop_ants[rpn->type])
     goto ret;
   goto *poop_ants[rpn->type];
   do {
+  ic_rdtsc:
+    AIWNIOS_ADD_CODE(X86Rdtsc, 0);
+    AIWNIOS_ADD_CODE(X86ShlImm, RDX, 32);
+    AIWNIOS_ADD_CODE(X86OrReg, RAX, RDX);
+    tmp.mode = MD_REG;
+    tmp.raw_type = RT_I64i;
+    tmp.reg = RAX;
+    code_off = ICMov(cctrl, &rpn->res, &tmp, bin, code_off);
+    break;
   ic_current_rip:
     if (rpn->res.mode == MD_REG)
       into_reg = rpn->res.reg;
