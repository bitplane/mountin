--- a/Src/PrsVar.HC
+++ b/Src/PrsVar.HC
@@ -605,7 +605,7 @@
 	    cc->flags|=CCF_DONT_MAKE_RES;
 	  }
-	  if (cc->flags&CCF_AOT_COMPILE)
-	    Free(tmpm->static_data);
+	  //AOT code generation serializes static_data after parsing completes.
+	  //It owns and frees the buffer once it has copied it into the module.
 	  break;
 	case PRS1B_LOCAL_VAR:
 	  if (mode&PRSF_UNION) {
--- a/Src/AIWNIOS_CodeGen.HC
+++ b/Src/AIWNIOS_CodeGen.HC
@@ -2220,7 +2220,9 @@
 			 for(idx=0;idx!=member_cnt;idx++) {
 			   if(mlst->flags&MLF_STATIC) {
 				 if(mlst->static_data) {
 					MemCpy(tmpgh->data+mlst->offset,mlst->static_data,mlst->member_class->size*mlst->dim.total_cnt);
+					Free(mlst->static_data);
+					mlst->static_data=NULL;
 				 }
 			   }
 			   mlst=mlst->next;
@@ -2229,6 +2231,8 @@
           tmpghr=CAlloc(sizeof CAOTHeapGlblRef);
           tmpghr->next=tmpgh->references;
           tmpgh->references=tmpghr;
+          foff=0;
+          WriteProtectMemCpy(misc->addr,&foff,8);
 	      //tmpghr->offset=misc->offset;
           tmpghr->rip=cc->aotc->rip+(misc->addr(U8*)-machine_code);
 	} else {
