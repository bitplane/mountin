--- a/Src/CMain.HC
+++ b/Src/CMain.HC
@@ -6,1 +6,2 @@ U8 *LexStmt2Bin(CCmpCtrl *cc,I64 *_type,I64 cmp_flags=0)
   LBtr(&cc->flags,CCf_PASS_TRACE_PRESENT);
+  cc->flags&=~CCF_RAW_AOT_ASM;
@@ -79,1 +80,1 @@ U8 *LexStmt2Bin(CCmpCtrl *cc,I64 *_type,I64 cmp_flags=0)
-      if(cc->flags&CCF_AOT_COMPILE)
+      if(cc->flags&CCF_AOT_COMPILE && !(cc->flags&CCF_RAW_AOT_ASM))
@@ -90,3 +91,8 @@ U8 *LexStmt2Bin(CCmpCtrl *cc,I64 *_type,I64 cmp_flags=0)
-	  k=(size+7)>>3;
-	  for (i=0;i<k;i++)
-	    AOTStoreCodeU64(cc,res[i]);
+          if (cc->flags&CCF_RAW_AOT_ASM)
+            for (i=0;i<size;i++)
+              AOTStoreCodeU8(cc,res(U8*)[i]);
+          else {
+            k=(size+7)>>3;
+            for (i=0;i<k;i++)
+              AOTStoreCodeU64(cc,res[i]);
+          }
