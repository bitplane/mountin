--- a/Src/AsmX64.HC
+++ b/Src/AsmX64.HC
@@ -1078,6 +1078,10 @@ I64 X86EncodeOpcode(I64 rip,I64 seg_size,CX86Opcode *opc,CX86AddrMode **modes,U
     to[len++]=0x66;
   }
 //Prefix group 4(Address size ovveride)
+  if (seg_size==16&&wants_modrm_sib ||
+        seg_size==64&&indir_mode&&indir_mode->short_addr==1)
+    to[len++]=0x67;
+
   if(rex&0x40) {
     to[len++]=rex;    
   }
