--- a/Src/PrsStmt.HC
+++ b/Src/PrsStmt.HC
@@ -1043,13 +1043,13 @@ Bool PrsStmt(CCmpCtrl *cc,I64 try_cnt=0,CCodeMisc *lb_break=NULL,
 		    if (tmpaot=PrsAsmBlk(cc)) {
 		      //If we are not in a function,we will jump past the assembly block
 		      // to avoid running it
-		      if(!cc->htc.fun) {
+		      if(!cc->htc.fun && !Bt(&cc->opts,OPTf_RAW_AOT)) {
 		        g_lb=COCMiscNew(cc,CMT_LABEL);
 		        RPNAddNew(cc,IC_JMP,g_lb,NULL,FALSE);
 		      }
 		      RPNAddNew(cc,IC_ASM,tmpaot,NULL,FALSE);
 		      cc->flags|=CCF_HAS_MISC_DATA;
-		      if(!cc->htc.fun)
+		      if(!cc->htc.fun && !Bt(&cc->opts,OPTf_RAW_AOT))
 		        RPNAddNew(cc,IC_LABEL,g_lb,NULL,FALSE);
 		    }
 #endif
@@ -1057,13 +1057,13 @@ Bool PrsStmt(CCmpCtrl *cc,I64 try_cnt=0,CCodeMisc *lb_break=NULL,
 		    if (tmpaot=PrsAsmBlk(cc)) {
 		      //If we are not in a function,we will jump past the assembly block
 		      // to avoid running it
-		      if(!cc->htc.fun) {
+		      if(!cc->htc.fun && !Bt(&cc->opts,OPTf_RAW_AOT)) {
 		        g_lb=COCMiscNew(cc,CMT_LABEL);
 		        RPNAddNew(cc,IC_JMP,g_lb,NULL,FALSE);
 		      }
 		      RPNAddNew(cc,IC_ASM,tmpaot,NULL,FALSE);
 		      cc->flags|=CCF_HAS_MISC_DATA;
-		      if(!cc->htc.fun)
+		      if(!cc->htc.fun && !Bt(&cc->opts,OPTf_RAW_AOT))
 		        RPNAddNew(cc,IC_LABEL,g_lb,NULL,FALSE);
 		    }
 #endif
@@ -1071,13 +1071,13 @@ Bool PrsStmt(CCmpCtrl *cc,I64 try_cnt=0,CCodeMisc *lb_break=NULL,
 		    if (tmpaot=PrsAsmBlk(cc)) {
 		      //If we are not in a function,we will jump past the assembly block
 		      // to avoid running it
-		      if(!cc->htc.fun) {
+		      if(!cc->htc.fun && !Bt(&cc->opts,OPTf_RAW_AOT)) {
 		        g_lb=COCMiscNew(cc,CMT_LABEL);
 		        RPNAddNew(cc,IC_JMP,g_lb,NULL,FALSE);
 		      }
 		      RPNAddNew(cc,IC_ASM,tmpaot,NULL,FALSE);
 		      cc->flags|=CCF_HAS_MISC_DATA;
-		      if(!cc->htc.fun)
+		      if(!cc->htc.fun && !Bt(&cc->opts,OPTf_RAW_AOT))
 		        RPNAddNew(cc,IC_LABEL,g_lb,NULL,FALSE);
 		    }
 #endif
