name: Publish Linux 6.12 AArch64 toolbox

on:
  workflow_dispatch:

jobs:
  publish:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_toolbox-publish.yml
    with:
      toolbox: docker:builder/compiler/linux/6.12/aarch64
