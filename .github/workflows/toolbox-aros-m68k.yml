name: Publish AROS m68k toolbox

on:
  workflow_dispatch:

jobs:
  publish:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_toolbox-publish.yml
    with:
      toolbox: docker:builder/compiler/aros/2026-09-24/m68k
