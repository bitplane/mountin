name: Publish AROS i386 toolbox

on:
  workflow_dispatch:

jobs:
  publish:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_toolbox-publish.yml
    with:
      toolbox: docker:builder/compiler/aros/2026-08-31/i386
